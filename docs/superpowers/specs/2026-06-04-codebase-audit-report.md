# CC Amazon Network — Codebase Audit Report
Date: 2026-06-04
Version audited: v1.3.37

Severity tags:
- [CRITICAL] — will crash or corrupt state at runtime
- [BUG]      — incorrect behavior, won't crash
- [PROTOCOL] — message sent but never handled, or handler with no sender
- [OVERSIGHT]— missing edge case, silent failure
- [PERF]     — blocking call, unbounded growth, unnecessary work
- [OPT]      — minor non-breaking improvement

---

## Layer 1: Turtle Runtime

### protocol.lua

- [BUG] `proto.send` — Hardcodes `proto.CH_SERVER` (1) as the reply channel for every transmit, regardless of the actual sender. This is correct for turtle→server traffic but wrong for server→turtle (CH_BROADCAST/CH_PRIVATE), warehouse (CH_WAREHOUSE), and turtle↔turtle (CH_LOCAL) messages: their `replyChannel` metadata falsely advertises channel 1. Currently latent because `proto.receive` discards the reply channel (`repCh` is captured but never used), so no code replies on it — but any future code that relies on `replyChannel` to route a response would send it to the server instead of the real sender. Should pass the sender's own listen channel as the reply channel.
- [OVERSIGHT] `proto.send` — Calls `textutils.serialise(msg)` on every send, while `proto.receive` accepts BOTH a raw table (`type(raw) == "table"`) and a serialised string. The send path never transmits a raw table, so the table branch in receive is dead unless some other (non-proto) sender transmits raw tables. Asymmetry is harmless but worth noting; serialise also throws on payloads containing functions/recursive tables, which would crash the sender rather than fail gracefully.
- [OVERSIGHT] `proto.receive` — `textutils.unserialise(raw)` returns `nil` on malformed input; the code guards with `local ok = msg ~= nil` and skips, so a corrupt/garbage modem message is silently dropped with no log. Acceptable, but failures are invisible for debugging.
- [OPT] `proto.receive` timer event — The destructure `local event, side, ch, repCh, raw = os.pullEvent()` reuses modem-message field names for the timer case, where the 2nd return is actually the timer ID. The check `event == "timer" and side == timer` is CORRECT (for a timer event `side` holds the timer ID), but the variable name `side` is misleading and invites future bugs. No functional issue. Naming it `p1`/`arg2` or handling timer with its own pull would be clearer.
- [OVERSIGHT] `proto.MSG` orphan-from-name scan — Most types map cleanly to a sender/handler. Two stand out by name: `ITEM_REQUEST`/`ITEM_READY` (older warehouse handshake) appear superseded by the newer `DELIVERY_ARRIVED`/`CHESTS_READY`/`ITEMS_READY`/`BATCH_DONE`/`ITEMS_DONE` handshake added below — possible dead/duplicate protocol pair to verify against actual senders/handlers. `TURTLE_QUERY`/`TURTLE_INFO` are documented as used by support turtles but the newer CH_LOCAL follow signals (`POSITION_UPDATE`, `ASCENDING`, `DESCENDED`) may make the server-mediated query path redundant. Flagged for cross-file confirmation in later tasks (handler/sender presence not verified here).
- No issue with the timer-ID check (Check 1), `proto.decode` MSG lookup (Check 4 — every `proto.MSG` entry has key == value, so `proto.MSG[msg.type]` validation succeeds for all valid types), terminate handling (Check 5 — `proto.receive` uses `os.pullEvent()`, the filtering form that re-throws `terminate` as an error, so Ctrl-T shutdown is NOT swallowed; `os.pullEventRaw` would have been the bug), or channel separation (Check 7 — channel is an explicit caller argument to `proto.send`, so CH_LOCAL messages are not auto-routed to CH_SERVER; only the reply-channel metadata is wrong, per the [BUG] above).

### waypoints.lua

- [OVERSIGHT] Hole coordinate literals duplicated across definitions — The X/Z literals `143`/`-2813` appear in `DISPATCH_HOLE`, `DISPATCH_STAGING` (x only), and `WORLD_EXIT`; `228`/`-2782` appear in both `ARRIVALS_HOLE` and `WORLD_ENTRY`. `WORLD_EXIT`/`WORLD_ENTRY` are by definition "below the dispatch/arrivals hole," yet they restate the raw coords instead of referencing `W.DISPATCH_HOLE.x/.z` and `W.ARRIVALS_HOLE.x/.z`. A single layout move of either hole requires editing the coordinate in two-to-three places, and forgetting one would put the underground entry/exit out of vertical alignment with its hole (turtle ascends/descends into a wall). Derive `WORLD_EXIT`/`WORLD_ENTRY` from the hole tables (only Y differs).
- [OVERSIGHT] Route builders deref `dock` fields with no internal nil guard — `internalReturnRouteFrom`, `returnAisleZ`, `dockFacing`, `departureRoute`, `supportDepartureRoute`, `returnRoute`, and `aisleZ` all access `dock.z` / `dock.x` / `dock.junction.x` without checking `dock` (or its fields) for nil inside the helper itself. All current callers are in `turtle_base.lua` and each guards at the call site with `if not _self.dock then return true end`, so no unguarded call path exists today — this is latent. Any future caller added without that guard would hit "attempt to index a nil value" with no clear error message pointing back to the missing dock assignment.

Checks that passed (no issue): single-axis waypoint rule in `internalReturnRouteFrom` (all 5 steps change exactly one of X/Z relative to the prior step, starting from `fromX,fromZ`); the `internalReturnRoute` compatibility wrapper (calls `internalReturnRouteFrom(dock, dock.junction.x, W.RED_Z)` — argument order matches the spec; its only quirk is a harmless duplicate first waypoint when `fromX==junction.x` and `fromZ==RED_Z`); `returnAisleZ` does not index by `dock.row` at all (it derives from `dock.z`), so an out-of-range/nil `row` is irrelevant to it; `dockFacing` returns only `2` or `0`, both valid {0,1,2,3}; and `CFG.FLOOR_Y` is referenced via the config constant everywhere inside this file (the raw `67` literal lives in `turtle_base.lua:622`, out of scope for this task).

### turtle_base.lua

Scope note: Checks 2 and 10 (`pushToBridge` / HTTP body read order) and Check 8 (`base.waitForMessage`) target functions that **do not exist in `turtle_base.lua`**. The bridge push and its `resp.readAll()`/`resp.close()` ordering live in `server.js`/`central_server.lua` (server side); the receive-with-timeout primitive lives in `protocol.lua` (`proto.receive`). They are flagged here as N/A for this file and deferred to the server/protocol audits.

- [OVERSIGHT] Internal GPS resync ignores failure silently — `applyMove` (line 172-174) calls `gpsSync()` every `GPS_RESYNC_INTERVAL` (50) moves but discards the return value. When `gps.locate()` returns nil (no satellite signal), `gpsSync` returns false and leaves `_self.pos` untouched (line 84-88), so the turtle keeps dead-reckoning with **no warning logged**. The same silent-fail pattern repeats at every bare `gpsSync()` call: `depart` (432), `returnToDock` (557, 575), `returnToDockInternal` (602, 618), and `base.init` (1063). The public `base.gpsSync` (line 92) does log a warning, but none of the internal callers use it. Not a crash (good — no nil index), but drift accumulates invisibly. Recommend logging a WARN on internal resync failure, at least on the precision-critical resyncs before bay navigation.

- [OVERSIGHT] `bypassForward` floor gate uses raw literal `66` instead of `CFG.FLOOR_Y` — Line 303 `if _self.pos.y < 66 then` guards the vertical (dig) bypass so the turtle never digs inside the surface depot. But the actual depot floor is `CFG.FLOOR_Y = 67` (waypoints.lua:11). A turtle standing on the floor is at y=67, so `67 < 66` is false → vertical bypass correctly skipped at the floor. However the threshold is off-by-one in intent: the comment says "Inside the surface depot (y >= 66) never dig", yet only y>=66 blocks it, meaning a turtle at **y=66** (one block below floor, e.g. mid-descent into a hole) is also treated as "depot" and refused a vertical bypass, while everything strictly below 66 (underground travel at y=60) is allowed. The threshold happens to work for the two real cases (floor=67 blocked, underground=60 allowed) but it is a hardcoded magic number that should be `_self.pos.y < W.DISPATCH_HOLE.y` or `< CFG.FLOOR_Y`. See also the duplicate-literal finding below.

- [OVERSIGHT] Hardcoded floor-Y literals `66`/`67` scattered instead of `CFG.FLOOR_Y` — Raw Y constants appear at: line 303 (`66`, bypass gate), 515 (`local FLOOR_Y = 67`, returnToDock), 622 (`move.to(..., 67, ...)`, returnToDockInternal drift correction), 1065 (`p.y >= 66`, init inside-building test), and 1133 (`_self.pos.y >= 67`, RECALL inside-building test). `waypoints.lua` exports `W.FLOOR_Y`/`CFG.FLOOR_Y = 67`, and `returnToDock` even defines its own local `FLOOR_Y = 67` rather than importing it. The init test uses 66 while the RECALL test uses 67 for the *same* "inside building" predicate (lines 1065 vs 1133) — an inconsistency: a turtle at exactly y=66 is "inside" for init homing but "outside" for RECALL, so RECALL would route it via the full underground `returnToDock` instead of the internal taxiway. Any floor-height change requires editing five places and they currently disagree.

- [OVERSIGHT] Building-bounds box duplicated and divergent — The "inside building" bounding box `x in [143,228], z in [-2817,-2782]` is hardcoded twice (init lines 1066-1067, RECALL lines 1134-1135) with the only difference being the Y test (66 vs 67, above). These magic coordinates duplicate `W.DISPATCH_HOLE`/`W.ARRIVALS_HOLE` extents from waypoints and would silently break if the depot footprint moves. Extract a single `W.isInsideBuilding(pos)` helper.

- [BUG] `returnToDockInternal` route failure leaves status stuck at RETURNING — Lines 614-616: if `move.followRoute(route)` fails partway (e.g. permanently blocked aisle), the function returns `false, "internal return failed: ..."` having already set `proto.STATUS.RETURNING` at line 609. Nothing resets the status or notifies the server, so the turtle reports RETURNING indefinitely while physically stuck mid-aisle. Same shape in `returnToDock` (lines 531, 572) and `depart` (443, 482) — all return false on route failure with status left mid-transition and no `sendFailed`/recovery. The caller (delivery/support job handler) must handle the false return; if it doesn't, the turtle is wedged. Flagged for cross-check against the role handlers.

- [BUG] No fuel reserve sized for the return trip — `fuel.isCritical()` triggers at a flat `FUEL_CRITICAL = 200` (line 14, 645). There is no calculation reserving enough fuel to reach the dock from the current position; 200 is a fixed floor regardless of how far away the turtle is. If a job takes the turtle far enough that it drops below 200 only after passing the point of no return, `ensureFuel` (859) will deploy the entangled chest mid-journey to recover — which works *only* if the chest has coal. If the chest is empty, `ensureFuel` enters an unbounded `while fuel.isCritical()` loop (line 860) that blocks the turtle forever (sending ERROR heartbeats every 5s, 6 iterations = 30s per outer retry cycle), never aborting the job or returning. Mid-job fuel exhaustion is therefore a soft-hang, not a clean failure. There is no give-up / escalate-and-park path, so the delivery pair is locked until a human refills the chest.

- [BUG] `ensureFuel` infinite block on empty chest — (detail of the above) Lines 860-881: the outer `while fuel.isCritical()` only exits when fuel is no longer critical. If `refuelFromChest` returns false (empty/unreachable chest), the inner for-loop runs 6 × `sleep(5)` (sending a heartbeat each iteration, one every 5s, 30s total per cycle) then re-loops the outer `while` forever. There is no max-retry / escalate-and-park path. Turtle stays "alive" to the server (heartbeats with STATUS.ERROR) but never progresses, permanently locking the delivery pair until human intervention.

- [OVERSIGHT] `requestItems` uses the legacy `ITEM_REQUEST`/`ITEM_READY` pair — Lines 1001-1012 still send `proto.MSG.ITEM_REQUEST` and await `proto.MSG.ITEM_READY`, the older warehouse handshake flagged as possibly superseded in the protocol.lua audit. Confirm whether any current job handler calls `base.requestItems`; if the live path uses the newer `DELIVERY_ARRIVED`/`CHESTS_READY`/`ITEMS_READY` handshake, this is dead code. [PROTOCOL] pending sender/handler confirmation.

- [OVERSIGHT] `_self.facing` persists across jobs and is never re-validated by GPS — Module-level `_self` (lines 24-39) holds `pos`, `facing`, `status`, `jobId`, `partnerId`, `busy`, `recalled`, `moveCount` for the whole process lifetime. `gpsSync` corrects `pos` but **never re-derives `facing`** (only the boot-time `detectFacing` does, line 104). If a chunk-unload/reload or a physical bump rotates the turtle without going through `move.turnLeft/Right`, `facing` silently desyncs from reality and every subsequent `applyMove` mistracks position until the next 50-move GPS resync masks it (position only, not heading). `move.to`'s per-step facing recalc partially self-heals X/Z drift, but a wrong heading still produces one wrong move per axis before correction. Re-running `detectFacing` after a GPS resync, or on job start, would harden this.

- Checks that passed (no issue):
  - Check 4 (move.to facing recalc) — CONFIRMED COMPLETE. The X phase (lines 395-399) and Z phase (lines 401-405) each call `move.face(...)` *inside* the while loop before every `move.forward()`, so a bypass overshoot self-corrects. The Y phase (lines 384-391) uses `move.up()/move.down()` which need no facing, so the "recalc each step" rule is N/A there — correct. The v1.3.31 fix is fully present.
  - Check 3a (bypass position tracking) — `bypassForward`'s `tryStrafe` and `tryVertical` call `applyMove(...)` after every successful `turtle.forward/up/down`, and `tryMove` deliberately does NOT call `applyMove` again when `bypassForward` returns true (line 334-336 returns immediately). Position accounting is consistent. Turns use `move.turnLeft/Right` which update `_self.facing`, and each strafe/vertical path restores its original facing via the paired `turnRtn()`/return-move before returning, so facing is restored (Check 3b OK).
  - Check 6a (returnToDockInternal nil dock) — guarded: line 600 `if not _self.dock then return true end`. Also handles already-at-dock (604-608).
  - Terminate/parallel structure — `base.run` uses `os.pullEvent()` (filtering form) so Ctrl-T terminate is honored; control loop and job runner share events via `parallel.waitForAny` correctly; RECALL on a busy turtle sets `_self.recalled` and lets the job coroutine do its own navigation rather than fighting over movement (lines 1121-1130) — correct separation.
  - `sendComplete`/`sendFailed` reset `status/jobId/partnerId/busy` (lines 976-994), so per-job state does not leak forward on the normal and failure paths; `_self.recalled` is cleared in `jobRunner` after every job (line 1179). `bypassAttempted`/`turtleWaits` are locals inside `tryMove`, no leak.

### delivery_turtle.lua

- [BUG] **`os.clock()` used for every deadline instead of `os.epoch("utc")` (Check 1 + 7).** Every timeout in the file is built on `os.clock()`, which returns CPU time and runs slow under parallel-coroutine load (the turtle runs `controlLoop` + `jobRunner` via `parallel.waitForAny`, plus this file's own nested wait loops). Affected sites: `waitForAny` (lines 25, 26, 29, 32), the queue/CHESTS_READY wait loop (lines 328 `deadline`, 329 `lastIReq`, 331, 333, 348, 351, 356). Real consequence: the "10 min" queue deadline (`os.clock() + 600`, line 328) and the 60s ITEM_REQUEST re-send interval (line 351) both expire *later* than intended in wall-clock terms, and the printed "%ds remaining" (line 347-348) understates how long the turtle has actually been waiting. Inconsistent with the warehouse side, which times out on wall-clock (`arrivalTimeout`/`msgTimeout`), so the turtle can still be "waiting" by its clock long after the warehouse has aborted the job. Should use `os.epoch("utc")/1000` (or `os.epoch("local")`) for all deadline math.

- [BUG] **First-phase (WAIT_ARRIVE) wait loop never breaks on RECALL (Check 3).** The queue/CHESTS_READY loop (lines 331-372) calls `waitForAny(...)`, and `waitForAny` *does* return `nil` immediately when `base.isRecalled()` (line 27). But the enclosing `while os.clock() < deadline` (line 331) has **no `isRecalled()` check** — when `waitForAny` returns nil the loop treats it as a normal "no message" tick: it re-sends DELIVERY_ARRIVED, increments `waitTick`, possibly re-sends ITEM_REQUEST, and loops again. So a RECALL received while the turtle is waiting for CHESTS_READY does **not** abort here; the turtle keeps pinging the warehouse for up to the full 600s deadline (or until `warehouse_chest_timeout`), spamming DELIVERY_ARRIVED the entire time. Meanwhile the control loop already fired `base.sendFailed("recalled")` (turtle_base.lua:1129) which cleared `partnerId`, status, and `busy` and told the server the job failed — so the turtle is reporting IDLE/failed to the server while its job coroutine is still actively looping and transmitting. It only escapes when the deadline elapses, then hits `warehouse_chest_timeout` and finally digs up the EC. Contrast the *second* phase loop (lines 431-472) which **does** check `base.isRecalled()` (line 438) at the top and breaks cleanly. Add the same `if base.isRecalled() then break end` to the first loop. (Note this is the loop the v1.3.35 "send DELIVERY_ARRIVED when no message" patch touched, so the regression is recent.)

- [OVERSIGHT] **`params.destination` / x,y,z nil check is absent (Check 6).** `local d = params.destination` (line 211) is dereferenced as `d.x`, `d.z` in the first `sendProgress` (line 259) and then `base.move.to(d.x, UNDERGROUND_Y, d.z)` (line 261). If `params.destination` is nil, line 259's `string.format(... d.x ...)` throws "attempt to index a nil value (local 'd')" before move is even reached. If `d` exists but `d.x`/`d.z` is nil, `move.to` compares `_self.pos.x ~= nil` → the `while _self.pos.x ~= tx` loop in turtle_base never terminates correctly / `_self.pos.y < ty` throws on the nil Y. Either way the job handler crashes; `jobRunner`'s `pcall` (turtle_base.lua:1178) catches it and calls `sendFailed(tostring(err))`, so it's a caught crash rather than a hang — but the failure reason is an opaque Lua error string, and the EC was never placed so no cleanup is needed. Recommend an explicit guard at job start: `if not d or not d.x or not d.y or not d.z then return base.sendFailed("missing_destination", false) end`.

- [OVERSIGHT] **Chest placement has no slot-occupied / wrong-item / inventory-full safety (Check 5).** (a) `placeChestAt` → `placeDownClear` (lines 40-43) digs whatever is below before placing, so a world block underneath is handled — but `turtle.placeDown()`'s return value is **discarded** at line 49 and 293; if placement fails (e.g. an unbreakable block, or an entity occupying the space) the code proceeds as though the chest is down and later `suckDown`/`dropDown` silently do nothing, stalling the handshake. (b) Inventory-full during the pull: `suckDown(1)` is called `chestCount` times (lines 384-386) but its return is only counted, never acted on — if slots 1-15 are unexpectedly full the pulled count is lower than requested and placement silently does fewer chests; the warehouse is told the real `#chestPositions` (line 419-421) so it's consistent, but the under-delivery is invisible. (c) **Wrong item type from the entangled chest:** the placement loop (lines 394-401) scans for `it.name:find("chest")`, so a non-chest item pulled out of the EC is simply skipped and left in inventory — it never gets placed and never gets dumped (the road-debris dump already happened at line 299-308, before the pull). That stray item then rides home in a non-protected slot and, on the *next* job, will be dropped into the next warehouse's EC as "debris." Low severity but a slow contamination path.

- [OVERSIGHT] **`turtle.placeDown()` return ignored for the entangled chest too (Check 5/10-adjacent).** Line 292-293 selects EC_SLOT and calls `placeDownClear()` without checking success. If the EC fails to place (block below couldn't be dug — bedrock, protected region, another turtle), the subsequent `dropDown` debris-dump (line 304) and every `suckDown` operate against air/no inventory, and the warehouse handshake stalls until the 600s queue timeout. CHECKPOINT 2 (PRE-PLACE, line 287) only verifies the chest is *in slot 16 before* placing, not that it actually went down.

- [OPT] **`lastIReq` is never reset once the first phase ends, but that is harmless (Check 8).** `lastIReq` (line 329) governs the 60s ITEM_REQUEST re-send and lives only inside the first wait loop (lines 331-372). The second phase (batch loop) re-sends `CHESTS_PLACED`, not `ITEM_REQUEST`, and never reads `lastIReq` again, so there is no stale-timer misfire across batches — the concern in the task description does not materialize because `lastIReq`'s scope is confined to the queue phase. No change needed; noted for completeness. (If the first loop is ever refactored to re-enter after a batch, reset `lastIReq` then.)

- [BUG] **`chestIdx` is never reset between batches, so later batches can't reuse earlier chests (Check, distribution logic).** `chestIdx` (line 429) starts at 1 and only ever *increments* (line 464) when a chest reports full. Across multiple ITEMS_READY batches, once `chestIdx` advances past a chest it never returns to it — even if a later batch is small and the current chest still has room, the turtle keeps filling from `chestIdx` forward. More seriously, when `chestIdx > #chestPositions` the inner `while chestIdx <= #chestPositions` (line 458) never executes, so a batch pulled into the turtle (line 453-455) is sucked up but **never dropped into any chest** — those items stay in turtle inventory, `BATCH_DONE` is still sent (line 470), and the items ride home undelivered. This is the failure mode when total item volume exceeds `#chestPositions` chests' capacity. At minimum the overflow case should be detected and reported (sendFailed / sendProgress), not silently swallowed.

- [PROTOCOL/OK] **JOB_ABORT to support on abort is handled by `base.sendFailed` (Check 4) — OK.** Every abort exit in this file goes through `base.sendFailed(...)` (lines 226, 253, 263, 277, 287's failure, 362, 376, 490) or the in-loop JOB_ABORT branch. `base.sendFailed` (turtle_base.lua:986-988) signals `JOB_ABORT` to `_self.partnerId` on CH_LOCAL before clearing it, and `support_turtle.lua:143-145` handles JOB_ABORT mid-job by returning to dock. On RECALL, the control loop calls `sendFailed("recalled")` itself (turtle_base.lua:1129), so the support turtle is released even though the delivery job coroutine is still (buggily, per the Check 3 finding) looping. So the support turtle is **not** orphaned on any abort path. No fix needed for Check 4 itself.

- [OK] **RETURN_TO_DOCK to support on normal completion (Check 9) — OK with one caveat.** Delivery clears `partnerId` at ascent (line 269) and restores it before descent (lines 506-507, guarded by `not isRecalled()`), so `base.returnToDock` captures a non-nil `savedPartnerId` and emits `RETURN_TO_DOCK` on CH_LOCAL after clearing the arrivals hole (turtle_base.lua:561-566). `support_turtle.lua:135` handles it. Caveat: on the RECALL/abort paths the descent restore (line 506) is skipped, so `partnerId` stays nil and `returnToDock` sends **no** RETURN_TO_DOCK — but that's correct here because the support turtle already received JOB_ABORT and is returning on its own. No orphan.

- [OVERSIGHT] **EC pickup on abort is best-effort and unverified (Check, cleanup).** The two first-phase abort exits (line 361 JOB_ABORT, line 375 timeout) do `turtle.select(EC_SLOT); turtle.digDown()` then immediately `sendFailed`, with **no `checkEC` verification** that the delivery ender chest actually came back (unlike the clean path, which has CHECKPOINT 3 PRE-RETURN at line 484 with a retry). If the turtle wasn't perfectly centred over the placed EC, `digDown` misses, the EC is left in the world, and the turtle returns to dock without its delivery chest — silently. Recommend routing these aborts through the same `checkEC("ABORT", ...)` recovery used on the clean path before returning.

- [OPT] **No `peripheral.wrap()` calls in this file (Check 10).** Delivery_turtle does all I/O via `turtle.*` builtins and `base.*` helpers; there are no `peripheral.wrap()` calls here, so the "wrap returns nil if not attached" hazard does not apply to this file. (Any peripheral wrapping happens in `turtle_base`/`protocol`, audited separately.) The modem itself is wrapped in `base.init`; if absent, `base.signalPartner*`/`sendToServer` no-op via the `if not _self.modem` guards rather than crashing.

- [OK] **Warehouse-side senders for all consumed messages exist (Check 2, cross-checked against warehouse.lua).** `CHESTS_READY` (warehouse.lua:317,332), `WAREHOUSE_QUEUED` (205,223,250,301), `ITEMS_READY` (378), `ITEMS_DONE` (367), and the `JOB_ABORT` abort path (`abortCurrent`, 359) all have real senders, and the turtle's emitted `DELIVERY_ARRIVED` / `CHESTS_PLACED` / `BATCH_DONE` / `ITEM_COLLECTED` / `ITEM_REQUEST` are all consumed there. The one asymmetry: the turtle ends the clean path with `ITEM_COLLECTED` (line 495), which the warehouse's `WAIT_DONE` state expects — names line up. No orphaned message in the delivery handshake. **FSM timeout summary:** WAIT_ARRIVE loop has a 600s deadline → `warehouse_chest_timeout` (good, but see the RECALL gap above); the batch loop (Phase 2) has **no overall deadline at all** — it loops `while true` with a 10s `waitForAny` and only exits on ITEMS_DONE, JOB_ABORT, or `isRecalled()`. If the warehouse goes silent mid-delivery (never sends ITEMS_DONE and never aborts), the turtle re-pings CHESTS_PLACED forever (line 443) with no escape — a stall-without-timeout [BUG]: Phase 2 should carry its own wall-clock deadline mirroring the warehouse's `msgTimeout`.

### support_turtle.lua

Scope note: support_turtle.lua is the thin job handler (155 lines); most of the actual CH_LOCAL movement logic (SUPPORT_STAGED handshake, ASCENDING/RETURN_TO_DOCK emission, POSITION_UPDATE broadcast) lives in `turtle_base.lua` `base.depart`/`base.returnToDock`/`applyMove`. Findings below cover both the handler and the base functions it relies on.

- [OVERSIGHT] **Movement blocks the message loop — abort/hold signals are queued, not real-time (Check 1, 3, 7).** The follow loop (lines 97-149) handles a message, then on POSITION_UPDATE calls `base.move.to(prev.x, prev.y, prev.z)` (line 121), which is a **blocking multi-step navigation** that does not poll the modem. While support is mid-`move.to`, any `ASCENDING`, `RETURN_TO_DOCK`, or `JOB_ABORT` sent by delivery is buffered in the modem event queue and only acted on at the *next* `proto.receive` after the move completes. In practice delivery only moves one block between POSITION_UPDATEs so `move.to` is usually a single step, but if support has fallen behind (missed updates) a single `prev` can be several blocks away, lengthening the window where it ignores ASCENDING. Worse for the abort case: support keeps following toward `prev` for the full duration of that move before it reads JOB_ABORT — it does not "stop mid-movement," it finishes the current move first. Not a crash, but the "hold immediately when delivery ascends" intent is not guaranteed.

- [BUG] **POSITION_UPDATE payload is not validated before use (Check 6).** Line 119-122 reads `local prev = msg.payload.prev` and guards only `if prev then`. It does not validate that `prev.x`, `prev.y`, `prev.z` are non-nil numbers. A malformed/truncated message (corrupt serialise, partial table) with `prev = {}` or `prev = { x=1 }` passes the `if prev` check and is handed to `base.move.to(nil, ...)`. In turtle_base, `move.to` runs `while _self.pos.x ~= tx` against `tx=nil`, which never equals the current integer position → the X-phase loop spins, calling `move.face`/`move.forward` with garbage and digging is disabled for support, so it will repeatedly bump a wall or loop until it wanders off-route. Should validate `type(prev.x)=="number"` etc. before moving. (Sender currently always populates all three fields at turtle_base.lua:179, so this is latent on corrupt-packet only — but support also never sanity-checks that `prev` is anywhere near its own position; a stale/duplicated packet could send it backward.)

- [PROTOCOL] **SUPPORT_READY is defined but never sent or handled.** `proto.MSG.SUPPORT_READY` ("support → delivery: I'm underground, proceed") exists in protocol.lua:49 and is in the documented CH_LOCAL flow, but `support_turtle.lua` never sends it and no file handles it (only protocol.lua and the plan doc reference the name). The actual underground handshake is done entirely via the SUPPORT_STAGED → descend-together path in `base.depart`; SUPPORT_READY is dead protocol. Either wire it in or remove it from the protocol table to avoid implying a handshake that does not exist.

- [PROTOCOL] **HOLE_READY / SUPPORT_STAGED naming is the inverse of the task description, but the implementation is internally consistent.** The task brief lists "SUPPORT_STAGED (support → delivery): descend now" and "HOLE_READY (delivery → support): come underground now." The code matches this direction: delivery sends HOLE_READY (turtle_base.lua:448) which support waits for (support_turtle.lua:51), and support sends SUPPORT_STAGED (turtle_base.lua:487) which delivery waits for (turtle_base.lua:459). No bug — flagged only because the brief's one-line glosses ("I'm 1 block behind you" for SUPPORT_STAGED) read as if support stages first; actual order is delivery reaches hole → HOLE_READY → support runs its departure route to staging → SUPPORT_STAGED → both descend.

- [OVERSIGHT] **SUPPORT_STAGED is fire-and-forget; no ACK, no confirmation delivery received it (Check 2).** In `base.depart` support side (turtle_base.lua:485-492), support sends SUPPORT_STAGED then `sleep(1)` and descends unconditionally — it never waits for an ACK. Good news: this means support does **not** stall forever waiting on delivery (the failure mode the check asks about cannot happen on support's side). Bad news: if the SUPPORT_STAGED packet is lost, delivery's wait loop (turtle_base.lua:454-465) burns its full 60s deadline and then **descends anyway** (the loop has no failure branch — it just falls through to the descend `for` loop at 468). So a lost SUPPORT_STAGED desyncs the pair by up to 60s but neither turtle aborts; they can collide at the hole since support sleeps only 1s while delivery may still be waiting. No timeout-to-abort on either side of this handshake.

- [OVERSIGHT] **HOLE_READY wait has a 180s timeout (good) but JOB_ABORT during it is the only early exit (Check 1, 3).** The pre-departure wait (lines 44-61) correctly handles both HOLE_READY and JOB_ABORT and times out at 180s → `sendFailed("no_hole_ready_from_delivery", true)` → returns to dock. This phase is well-guarded. Note it does **not** handle RECALL: a RECALL arriving while support waits for HOLE_READY is processed by the control loop (sets `_self.recalled`, calls `sendFailed`), but this job loop has no `base.isRecalled()` check, so it keeps waiting up to 180s while the server already thinks the job failed — same class of bug as the delivery_turtle Phase-1 RECALL gap. The follow loop (lines 97-149) also has no `isRecalled()` check.

- [OK] **JOB_ABORT is handled in both phases and returns cleanly (Check 3).** Pre-departure: line 55-58 breaks and `returnToDock()`. Follow phase: line 143-145 breaks to the `returnToDock()` at line 153. Because digging is disabled and `returnToDock` self-navigates underground → arrivals hole → up → dock, abort returns the support turtle home. The only caveat is the blocking-move delay noted above (it finishes the in-flight `move.to` before seeing the abort). It does not get permanently stuck.

- [OK] **RETURN_TO_DOCK flow uses returnToDock from turtle_base (Check 4).** Line 135-141 handles RETURN_TO_DOCK: it `sleep(8)` to let delivery clear the arrivals hole, breaks, and falls through to `base.returnToDock()` (line 153). `returnToDock` (turtle_base.lua:510) takes support from its current underground position to the arrivals hole, ascends, navigates the red taxiway to its dock, and tops up fuel via `dockRefuel` (line 589-591, gated on `not _self.canDig` which is true for support). Correct and independent. Caveat: the hardcoded `sleep(8)` is a guess at how long delivery needs to clear — if delivery is slow, support could still ascend into it; there is no positional confirmation.

- [BUG] **No fuel check before the follow sequence; underground exhaustion soft-hangs (Check 5).** `support_turtle.lua` never checks fuel at job start — there is no `fuel.isCritical()` / reserve calc in the handler. The only fuel safety is `controlLoop`'s `if fuel.isCritical() then fuel.ensureFuel() end` (turtle_base.lua:1096), which runs between events. If support runs low underground mid-follow, `fuel.ensureFuel` (turtle_base.lua:859) tries `refuelFromChest`; support's fuel ender chest is in CHEST_SLOT and `ensureFuel` will place/use it. But if that chest is empty, `ensureFuel` enters the same unbounded `while fuel.isCritical()` block flagged in the turtle_base audit — support parks underground sending ERROR heartbeats forever, and since it has stopped moving, the delivery turtle it was chunk-loading loses its loader. There is no pre-flight "enough fuel to follow + return" reserve. `dockRefuel` only runs at the dock (init and on return), not before each job's departure, and only tops up if below 80% — so a job dispatched right after a long previous trip could start with marginal fuel.

- [OVERSIGHT] **Stall guard exists but depends on the server being up (Check 7).** When `proto.receive` times out (15s, line 98) and the server is reachable, support calls `base.queryTurtle(partnerId, 5)` (line 105) and breaks to return home if the partner is offline or has no jobId — this is a real stall guard against a silently-crashed delivery turtle. However: (a) if `base.isServerDown()` is true, it just `sleep(2)` and loops with **no** partner liveness check (lines 102-104), so during a server outage a crashed delivery partner leaves support following nothing, waiting indefinitely; (b) `queryTurtle` relies on the server's view of the partner being fresh — if the server hasn't yet marked the crashed delivery offline (heartbeat timeout window), `info.online` is still true and support keeps waiting another 15s+ per cycle. The guard is reasonable for the normal case but has no absolute wall-clock ceiling on the follow phase (unlike the 180s HOLE_READY wait).

- [OK] **Modem channels are opened correctly (Check 8).** `base.init` → `proto.openChannels(modem, { CH_BROADCAST(2), CH_PRIVATE(3), CH_LOCAL(5) })` (turtle_base.lua:71). CH_LOCAL is opened, so the turtle-to-turtle follow/handshake traffic is received. CH_SERVER(1) is the transmit/reply channel (turtles only listen on broadcast/private/local), and CH_WAREHOUSE(4) is correctly not opened by turtles. `proto.receive(base.getSelfId(), 5)` in the handler filters by self-id via the decode path. Channels are never explicitly closed, but the turtle process is long-lived (loops jobs forever), so that is acceptable.

- [OPT] **Debug `print` of params on every job (lines 26-33) and several `print` calls in the hot follow loop.** Harmless but noisy; the param dump prints `dest=NIL` as a normal case ("server may need update"), suggesting `destination` is not actually consumed by support — `dest` is read into a local at line 20 and only ever printed, never used for navigation (support follows POSITION_UPDATEs, not the destination). The `destination` param plumbing into support is dead for movement purposes.

- [OPT] **`POLL_INTERVAL` (line 10) and `UNDERGROUND_Y` (line 9) are declared but unused.** `POLL_INTERVAL = 5` is never referenced (the follow loop uses the 15s receive timeout and a 5s queryTurtle timeout instead). `UNDERGROUND_Y = 60` is also unused (the underground Y is owned by turtle_base via `W.WORLD_EXIT.y`). Dead locals; remove or wire in for consistency with the comment.

---

## Layer 2: Server Layer

### central_server.lua

- [CRITICAL] **`registry.checkTimeouts` and `registry.markOffline` never reset a stuck turtle's status — and the reassigned job's support partner is never recalled (Check 5, 6).** `checkTimeouts` (lines 177-188) marks the turtle offline and calls `jobQueue.reassign(t.jobId, ...)`, but `reassign` (369-378) only touches the *job* (`status=PENDING`, `assignedTo=nil`); it does NOT reset the offline turtle's `status`. `markOffline` (166-175) explicitly leaves status untouched too. So a turtle that dies mid-job stays at `TRAVELLING`/`RETURNING` in the registry forever (only `online=false` flips). That alone is cosmetic, BUT the reassigned DELIVER job has a `linkedJob` support job that is never cancelled/reassigned: the now-orphaned support turtle keeps its `SUPPORT_FOLLOW` job ASSIGNED, stays non-IDLE, and `destinationBusy`/`getIdle` exclude it — so the freshly re-queued delivery can be paired with the *same* still-following support turtle's slot only if it happens to come free, otherwise dispatch stalls. Net effect: heartbeat-timeout cleanup frees the delivery job but orphans the support side. Compare `jobQueue.fail` (348-367) and `cancelJob`, which DO reset `t.status=IDLE`. `reassign` should do the same and should cancel/reassign `job.linkedJob`.

- [BUG] **`pushToBridge` can re-enter and double-dispatch dashboard commands (Check 1).** The bridge timer fires every `BRIDGE_INTERVAL` (2s) and the main loop does `pcall(pushToBridge); bridgeTimer = os.startTimer(...)` (lines 1010-1012). `http.post` in CC is **blocking/yielding** — the function suspends at line 953 until the HTTP round-trip completes. While suspended, `os.pullEvent()` continues to dispatch other events, and the bridge timer is *not yet restarted* (the restart is after the post returns), so a second `pushToBridge` cannot be launched by the timer during the in-flight call — good, no concurrent push. HOWEVER, the real exposure is latency: if the Node bridge is slow (>2s), each push blocks the entire server event loop (no heartbeat ACKs, no dispatch, no console) for the duration. And because the commands come back in the response body and are executed via `pcall(handleBridgeCommand, cmd)` (962-964), if the *bridge* returns the same queued command twice (e.g. it didn't clear its queue because a prior push's body read failed), the command is executed twice — there is no idempotency key or de-dup on the CC side. CANCEL_JOB/DISPATCH_DELIVERY are not idempotent (DISPATCH_DELIVERY would create two jobs). De-dup is the bridge's responsibility but the server trusts it blindly.

- [PERF] **`http.post` blocks the single-threaded server loop (Check 1, detail).** Confirmed above: there is no `parallel` wrapper around the bridge push. Every 2s the whole `server.run` loop stalls for one full HTTP round-trip. On localhost this is sub-ms, but any bridge stall (Node GC pause, disk write) directly delays heartbeat ACKs and can push turtles toward their `HEARTBEAT_TIMEOUT`. Consider running the bridge push in its own coroutine via `parallel.waitForAny`, or using `http.request` (async event-driven) instead of blocking `http.post`.

- [OK] **`pushToBridge` reads body before close, and wraps decode + each command in pcall (Check 2).** Confirmed the v1.3.32 fix: `local body = resp.readAll()` (958) runs BEFORE `resp.close()` (959). The whole push is wrapped in `pcall(pushToBridge)` at the call site (1011), the JSON decode is `pcall(textutils.unserialiseJSON, body)` (960), and each command dispatch is `pcall(handleBridgeCommand, cmd)` (963). A failing command cannot kill the loop or abort the remaining commands. Error path logs `Bridge push failed` on `resp==nil` (967). This check passes cleanly.

- [OK] **DISPATCH_DELIVERY validates x/z before submitting (Check 3).** Lines 838-850: `x=tonumber(p.x)`, `z=tonumber(p.z)`, `y=tonumber(p.y) or 67`, then `if not (x and z) then ... return end`. An empty-string coord → `tonumber("")` → nil → ignored with a WARN. Y defaults to 67 (floor) if absent/invalid. Solid. (Minor [OPT]: a malformed numeric Y like `"abc"` silently becomes 67 rather than erroring — acceptable since Y is non-critical for destination matching.)

- [BUG] **`CLEAR_JOBS` and `RESET_STATUS` iterate the live table while mutating registry entries — RESET_STATUS is fine, but CLEAR_JOBS does NOT remove individual jobs during the loop so the pairs() snapshot concern is moot here, yet it has a different flaw (Check 4).** CLEAR_JOBS (898-912) iterates `pairs(state.jobs)` and only *reads* each job + mutates `state.registry` entries inside the loop; it sets `state.jobs = {}` AFTER the loop (910), not via `table.remove`/nil-assignment during iteration, so there is no skip/double-visit hazard — the pairs() concern from the task does not apply. The real issue: it recalls turtles for jobs in `ASSIGNED`/`IN_PROGRESS` but does NOT cancel the matching `linkedJob` relationship or guard against double-recalling a turtle that appears as `assignedTo` on both a DELIVER and its linked SUPPORT job — a turtle can receive two RECALL messages (harmless, idempotent on the turtle side) and the order of recall vs. wiping `state.jobs` means any in-flight warehouse handshake for those jobs is now orphaned (warehouse still holds the job, server forgot it → next CHESTS_READY hits the `fwdToTurtle` NOT-FOUND path and sends JOB_ABORT to warehouse, which is actually the correct recovery). Net: functionally OK but relies on the warehouse-abort fallback to clean up. No crash.

- [BUG] **Job double-assignment guard is destination-based only and racy across a single tick is prevented, but ACK-timeout re-queue can pair a second turtle to a destination already IN_PROGRESS elsewhere (Check 7).** `dispatcher.tick` dispatches at most ONE pair per tick (`return` at 518) and `destinationBusy` (422-433) skips a pending job whose (x,z) already has an ASSIGNED/IN_PROGRESS job — so two turtles cannot be sent to the same destination in normal flow, and a single turtle cannot be assigned two jobs because once assigned it leaves `getIdle` (status≠IDLE). HOWEVER: `destinationBusy` only checks `j.params.destination.x/.z`. The internally-created SUPPORT_FOLLOW job (473-476) has `params={partnerId,masterJobId}` with **no destination**, so it's invisible to `destinationBusy` (correctly). The gap: if a DELIVER job ACK-times-out (`checkAckTimeouts` 394-404 → `reassign` → status PENDING) while its turtle is actually still travelling (slow ACK), the destination is now PENDING not ASSIGNED, so `destinationBusy` returns false and a *second* pair can be dispatched to the same destination — two delivery turtles converge. `ACK_TIMEOUT=10s` vs a multi-minute trip makes this plausible if the JOB_ACK packet is lost. The same-turtle case is safe; the same-destination case is not fully guarded under ACK loss.

- [OVERSIGHT] **`sendTo` for an unknown turtle silently transmits into the void (Check 8).** `sendTo` (55-58) calls `proto.encode(..., turtleId, ...)` then `proto.send` (modem.transmit) with no registry lookup. If `turtleId` is nil or unknown it does NOT throw — it just broadcasts a private-channel message addressed to a non-existent recipient, which every turtle's decode filters out by `to`. Callers that matter mostly guard first (`handleBridgeCommand` RECALL/SET_IDLE check `state.registry[tid]` before calling, lines 854/881), but `jobQueue`/`cancelJob`/`fwdToTurtle` call `sendTo(job.assignedTo, ...)` where `assignedTo` could be a stale id of a now-removed turtle — silent no-op, no log. Low risk but a lost message is invisible. Consider a `if not state.registry[turtleId] then logWarn(...) end` in `sendTo`.

- [OVERSIGHT] **`saveJobs` / `loadJobs` — load is corruption-safe, save is not atomic and not pcall-wrapped (Check 9).** Path is the bare relative `"jobs.dat"` (192) in the CC computer's root — safe within the sandbox, no traversal. `loadJobs` (216-248) guards: `fs.exists` check, `fs.open` nil check, and `if type(data) ~= "table" then return end` after `unserialise` — a corrupt/garbage file is handled gracefully (no crash, jobs simply not restored). GOOD. BUT: (a) `saveJobs` (194-214) is not wrapped in pcall and `textutils.serialise` throws if any job param contains a function/recursive table or non-serialisable value — that would propagate up; most call sites are inside the dispatcher pcall but `jobQueue.add`→`saveJobs` runs inside `server.submitJob`'s `pcall(dispatcher.tick)` only for the tick, not the `add` itself (the `add` saveJobs at 282 is NOT pcall-protected when called from the console `job` command path → a serialise failure there crashes the console handler, caught by... nothing at the console level, but the main loop's char/key handling has no pcall). (b) `saveJobs` writes in place (open "w" truncates then writes); a crash/chunk-unload mid-write leaves a truncated `jobs.dat` — but loadJobs' type check catches that on next boot. (c) `loadJobs` does not re-derive `jobCounter` from restored job IDs beyond reading the saved counter, so if `jobs.dat` is hand-edited or the counter desyncs, `newJobId` could collide with a restored id (overwriting a live job in `state.jobs[id]`). Minor.

- [BUG] **`state.registry` grows unbounded — offline turtles are never removed (Check 10).** `registry.markOffline` only sets `online=false`; there is no path that ever does `state.registry[id] = nil`. The "explicit recall + remove" mentioned in the markOffline comment (172-173) **does not exist** anywhere in the file — there is no `registry.remove`, and no bridge/console command deletes a turtle. Every turtle that ever registers stays in `state.registry` for the life of the process, and every offline turtle is pushed to the dashboard each cycle (`pushToBridge` iterates all of `state.registry`, 925) so the dashboard fleet list accumulates ghosts. Re-registration of the same id reuses the slot (register overwrites `state.registry[id]`), so churn of a *fixed* fleet is bounded by fleet size — the unbounded-growth risk is only if turtle IDs are non-deterministic across reboots (they are stable `os.getComputerID`-based, so practically bounded). Still: no decommission path, and offline ghosts never disappear from the dashboard.

- [OVERSIGHT] **`registry.checkTimeouts` forward-references `jobQueue` before it is assigned at module-load — safe only because of call ordering (Check, init).** `registry.checkTimeouts` (line 183) calls `jobQueue.reassign`, but `jobQueue` is a global declared at line 261, AFTER `registry` (67). Because `checkTimeouts` is only *invoked* at runtime (via the health timer, well after module load), the global `jobQueue` is populated by then — works. But `jobQueue` is an implicit global (no `local`), polluting `_G`; if any other loaded module defined `jobQueue` it would clash. Should be `local jobQueue` declared up top.

- [OK] **`cancelJob` does all three things (Check 6).** `server.cancelJob` (671-688): (a) sends RECALL to `assignedTo` (678), (b) resets `t.status=IDLE; t.jobId=nil` (680), (c) sets `job.status=CANCELLED` (682) — note it does NOT delete the job from `state.jobs` (leaves it as a CANCELLED record, which is fine for history/dashboard) and recursively cancels `linkedJob` (685). The recursion is safe from infinite loop because the support job's `linkedJob` is nil (only the DELIVER job links to support, not vice-versa) — confirmed: support jobs are created with `params={partnerId,masterJobId}` and never get `.linkedJob` set. Solid.

- [OVERSIGHT] **`jobQueue.complete` does not free the linked support turtle (Check 6-adjacent, by design but fragile).** Lines 341-345 deliberately skip auto-completing the linked support job (documented reason: support is still physically returning). This is correct, but it means a support turtle whose master delivery completes relies ENTIRELY on the support turtle sending its own JOB_COMPLETE when it docks. If that support turtle crashes/loses-its-loader between master-complete and docking, its SUPPORT_FOLLOW job stays IN_PROGRESS forever (until heartbeat timeout → reassign, which as noted in the CRITICAL above does not reset its status). The support job has no destination so it is never naturally garbage-collected. A stale IN_PROGRESS support job keeps that turtle excluded from `getIdle`.

- [OPT] **`console handleConsoleEnter` runs with no pcall (Check, robustness).** The main loop dispatches `key`/`char` events (1024-1035) without pcall; a throw inside `handleConsoleEnter` (e.g. a `submitJob`→`saveJobs` serialise error, or a malformed command) would crash `server.run` entirely. Wrap console handling in pcall like the timer/handler paths.

Checks that passed with no issue: bridge body-before-close + nested pcall (Check 2, confirmed v1.3.32 fix present); DISPATCH_DELIVERY coord validation (Check 3); RESET_STATUS live-table mutation is safe (only mutates values, never structure); cancelJob completeness + recursion safety (Check 6); same-turtle double-assignment is prevented by the IDLE filter (Check 7); loadJobs corruption guard (Check 9).

### warehouse.lua

- [BUG] **Auto-queue de-dups by `turtleId`, not `jobId` — a stale queue entry for a cancelled+reissued job blocks the new delivery (Check 1).** The ITEM_REQUEST handler de-duplicates on `jobId` (lines 201, 211), but the v1.3.35 DELIVERY_ARRIVED auto-queue de-duplicates on `turtleId` (lines 236-241: `e.turtleId == msg.from`). Scenario: a job is cancelled server-side and re-issued to the *same* turtle with a new `jobId`, while a stale queue entry (old jobId, same turtleId) still sits in `queue`. When DELIVERY_ARRIVED arrives carrying the **new** jobId, the `turtleId`-based `known` check matches the stale entry, so the new delivery is **not** auto-queued. The warehouse eventually serves the stale entry, sends CHESTS_READY/ITEMS_READY/ITEMS_DONE with the **old** jobId, and the server's `fwdToTurtle` (central_server.lua:612-625) can't find the old job → replies JOB_ABORT to the warehouse → `abortCurrent` sweeps EC and returns to IDLE. The real (new) job is never served by this DELIVERY_ARRIVED, so the turtle must re-ping. Net: the stale `turtleId` match orphans the new jobId until a later DELIVERY_ARRIVED re-ping (in WAIT_ARRIVE) or queue timeout clears it. The auto-queue should match on `jobId` like ITEM_REQUEST does, OR additionally update/replace a same-turtle entry whose jobId differs. (Note also: the ITEM_REQUEST path keys on jobId while DELIVERY_ARRIVED keys on turtleId, so the same turtle with a *new* job can be queued twice if its first entry came in via ITEM_REQUEST under a different jobId — divergent de-dup keys are the root issue.)

- [BUG] **`clearEnderChest` is single-shot with no polling, no max wait, and no verification the EC actually emptied (Check 2).** There is no "wait for EC to clear" loop anywhere — `clearEnderChest` (lines 65-83) lists the chest **once**, issues one `rsBridge.importItem` per stack, logs a total, and returns. It is called synchronously before `loadChests` (line 311) and on every abort/sweep. Consequences: (a) **No polling interval / no max-wait** exist — the task's assumption of a timed clear loop does not match the code. (b) If `rsBridge.importItem` cannot move an item (RS network full, item not insertable, partial move), the leftover **silently remains** in the EC: the function does not re-`list()` to confirm empty, does not retry, and does not warn that residue is left. The next `loadChests` then exports chests into a chest that still holds stray items, and on the next job those strays get pulled by the turtle as "debris." (c) `importItem` is passed `count = item.count` from the snapshot `list()`; if the EC contents changed between list and import (a turtle still pulling) the count is stale. Recommend: loop `list()` → import → re-`list()` with a bounded retry count and a WARN-and-continue if non-empty after N tries.

- [BUG] **No timeout on the warehouse-internal handshake when the server/turtle goes silent in `WAIT_DONE` is handled, but the IDLE→serve transition emits CHESTS_READY only after DELIVERY_ARRIVED with no guard that the turtle is still alive — and the arrival timeout aborts without notifying the turtle (Check 3, state map).** State machine timeout map:
  - **IDLE** — advanced by: a non-empty `queue` (line 297). Timeout: none (correct; resting state). Picks `queue[1]`, sends WAREHOUSE_QUEUED(position=0), → WAIT_ARRIVE.
  - **WAIT_ARRIVE** — advanced by: DELIVERY_ARRIVED from `current.turtleId` (line 308). Timeout: `arrivalTimeout` = 1800s (line 322) → `abortCurrent("arrival timeout")`. On timeout: aborts queue entry, sweeps EC, returns to IDLE. **OK**, but `abortCurrent` sends **no JOB_ABORT to the turtle/server**, so a turtle that is actually still travelling (legitimately slow) gets dropped from the queue silently; its next DELIVERY_ARRIVED re-queues it at the back via auto-queue, which works but loses position.
  - **WAIT_PLACED** — advanced by: CHESTS_PLACED (line 336). Timeout: `msgTimeout` = 120s (line 358) → `abortCurrent("CHESTS_PLACED timeout")`. **OK** (covers Check 7 turtle disconnect — see below).
  - **SEND_BATCH** — advanced by: immediate transition (no message). Timeout: none, but it never waits — it either exports a batch → WAIT_BATCH or sends ITEMS_DONE → WAIT_DONE on the same tick. **OK** (no stall possible).
  - **WAIT_BATCH** — advanced by: BATCH_DONE (line 390). Timeout: `msgTimeout` = 120s (line 392) → `abortCurrent("BATCH_DONE timeout")`. **OK**.
  - **WAIT_DONE** — advanced by: ITEM_COLLECTED (line 403). Timeout: `msgTimeout` = 120s (line 407) → sweeps EC, `current=nil`, IDLE (does NOT call `abortCurrent`, so no JOB_ABORT either, but the job is effectively done). **OK**.
  No state stalls forever — every waiting state has a wall-clock timeout driven by the 1s `tickTimer` (line 434), so even with zero modem traffic the timeouts fire. The only gap is that timeout-aborts are silent to the turtle/server (no JOB_ABORT emitted from `abortCurrent`), so the counterparty isn't told the warehouse gave up.

- [OK] **`chestsNeeded` handles fractional results correctly (Check 4).** Lines 57-63: per-item `math.ceil(count / 64)` correctly rounds a partial stack up (65 items → `ceil(65/64)` = 2 stacks, not 1), summed across items, then `math.ceil(totalStacks / 27)` rounds the chest count up (a vanilla chest = 27 slots), clamped to `[1, maxChestsPerDelivery=6]`. Math is correct for the slot model. Minor caveat: it assumes one slot per stack regardless of stackability — non-stackable items (count>1 but max-stack=1) would each need their own slot, undercounting chests, but the turtle distributes greedily so this only risks under-provisioning chests for exotic items; acceptable for the current item set.

- [OK] **Queue advances correctly on completion; a concurrent DELIVERY_ARRIVED for `current` does not double-queue (Check 5).** On ITEM_COLLECTED (WAIT_DONE, line 403-406) the warehouse sets `current=nil`, clears batches, enters IDLE; the next `tick()` (fired by the 1s timer or the next message) pops `queue[1]` and starts serving it (lines 296-304). On the ITEMS_DONE→WAIT_DONE path the same applies. Race check: a DELIVERY_ARRIVED arriving **while serving** `current` is caught by the auto-queue `known = current and current.turtleId == msg.from` guard (line 236), so it is not re-queued; it then falls through to `inboxPut` (line 285 via comment at 256) and is consumed as a benign re-ping in WAIT_PLACED (line 329) or ignored in other states (it sits in the inbox bucket and is only `inboxGet`-matched in WAIT_ARRIVE/WAIT_PLACED). No double-service. **One latent leak**: if a DELIVERY_ARRIVED for the *currently-served* turtle is buffered but never consumed (e.g. arrives during WAIT_BATCH/WAIT_DONE), it stays in `inbox[DELIVERY_ARRIVED]` forever — the bucket is never swept on job completion, so stale entries accumulate across many jobs ([PERF]/[OVERSIGHT], slow unbounded inbox growth; the next job for the same turtleId could even mis-consume an old DELIVERY_ARRIVED in WAIT_ARRIVE and skip its real arrival wait).

- [OK] **`sendToServer` uses the correct channel and the server routes by jobId, not the `to` field (Check 6).** `sendToServer` (lines 52-55) transmits on `proto.CH_SERVER` (1), which is correct: the **server** listens on CH_SERVER (central_server.lua:828) and forwards warehouse replies to the turtle via `fwdToTurtle`, which routes by `payload.jobId → job.assignedTo` (central_server.lua:612-617), NOT by the message `to` field. The warehouse receives server-forwarded messages on CH_WAREHOUSE (4), which it opens at line 46. So the channel pairing is correct (warehouse TX on CH_SERVER, RX on CH_WAREHOUSE; server TX on CH_WAREHOUSE, RX on CH_SERVER). [OPT] The `toId` argument passed to `sendToServer` (the turtle id) is effectively **ignored** by the server's routing (it routes solely on `jobId`); it's only cosmetic/log metadata. Every warehouse→turtle message therefore MUST include a valid `jobId` in the payload — they all do (lines 206, 224, 251, 302, 318, 333, 367, 378, 388, 401). If a payload ever omitted jobId, the server would hit the NOT-FOUND→JOB_ABORT path.

- [OK] **Turtle disconnect after CHESTS_READY is covered by the WAIT_PLACED timeout (Check 7).** After CHESTS_READY is sent (line 317), the warehouse enters WAIT_PLACED and waits for CHESTS_PLACED with a 120s `msgTimeout` (line 358). If the turtle disconnects/crashes before CHESTS_PLACED, the timeout fires → `abortCurrent("CHESTS_PLACED timeout")` sweeps the EC (recovering the loaded chests back into RS) and returns to IDLE. It does **not** wait forever. Caveat (shared with Check 3): the abort is silent to the turtle/server — no JOB_ABORT is sent from `abortCurrent`, so if the turtle reconnects after the abort it will re-ping DELIVERY_ARRIVED and be auto-queued fresh (correct recovery), but the loaded chests were already swept so it restarts cleanly.

- [BUG] **Item export does not verify stock atomically and silently under-delivers; a requested item missing from RS is only WARN-logged, never aborted (Check 8).** `checkStock` (lines 97-104) reads `rsBridge.getItem().amount` and logs a WARNING if `have < needed`, but **returns regardless** and the caller ignores the return (line 342 calls it purely for the side-effect log). `exportItem` (lines 92-95) then exports as many as RS has; if RS has fewer than requested, `moved < entry.count` and a "Short: m/n" line is logged (line 374-375) but the job **proceeds** — the turtle is told ITEMS_READY and pulls whatever is in the EC, under-delivering silently to the destination. There is no abort, no re-queue, no message back to the server flagging the shortfall. A completely-missing item (`getItem` returns nil → `have=0`) likewise only WARNs and continues with zero delivered for that item. Also: stock is checked at batch-build time (line 342, in WAIT_PLACED) but exported later batch-by-batch in SEND_BATCH — between check and export, another consumer could drain RS, so even a passing `checkStock` doesn't guarantee the export. Recommend: on a hard shortfall, either abort the job with a distinct reason the server can surface, or include the actually-delivered counts in ITEMS_DONE so the dashboard can flag partial deliveries.

- [BUG] **`position` in WAREHOUSE_QUEUED is 1-indexed and correct at enqueue time, but it is a stale snapshot that is never re-sent as the queue drains (Check 9).** `position = #queue` (lines 224, 251) is sent once, at the moment of enqueue, and equals the entry's 1-based slot (the entry was just `table.insert`ed to the end, so `#queue` is its index). `position = 0` (lines 206, 301) correctly means "now being served." **The bug**: as earlier entries are served and `table.remove(queue, 1)`'d (line 298), every still-waiting entry's real position decreases, but the warehouse **never re-emits an updated WAREHOUSE_QUEUED** to those waiting turtles. A turtle queued at position 3 is told "3" once and keeps displaying 3 even after two ahead of it finish and it's actually next. So position is correct *only* at the instant of queuing; it goes stale immediately and is never refreshed (except on a duplicate ITEM_REQUEST, which re-sends the *current* `#queue`-based position only if it re-runs the enqueue path — but a duplicate for an already-queued job hits the early-return at line 212 and re-sends nothing). Recommend broadcasting updated positions to all waiting entries whenever the queue head advances.

- [OVERSIGHT] **Logging covers state *entry* and aborts well, but there is no periodic "stuck in state X for Ys" heartbeat, so a silent stall is only diagnosable by inferring from the last log line (Check 10).** Positives: every state transition logs (Serving, "Turtle arrived", "CHESTS_READY sent — waiting for CHESTS_PLACED", "Chests placed — N batches", "Batch i/N exported — waiting for BATCH_DONE", "All batches done — waiting for ITEM_COLLECTED", "Job complete"), and every abort logs its reason with the jobId. So an operator reading the log can see which `wait`-state the warehouse last entered. **Gaps**: (a) once a state is entered, the warehouse goes **completely silent** until either the awaited message or the timeout (up to 120s, or 1800s in WAIT_ARRIVE) — during a stall there is no "still waiting for CHESTS_PLACED (45s elapsed)" tick, so a live operator watching can't distinguish "hung" from "working." (b) There is no log of `state`/`stateTs`/`#queue`/`current.jobId` on a timer, and no `STATUS`/heartbeat sent to the server, so the dashboard has zero visibility into warehouse state (the warehouse is not in the registry). (c) The `tick()` IDLE branch returns silently when `#queue==0` (line 297), which is fine, but there's no startup/periodic "IDLE, queue empty" so a wedged-in-IDLE-with-a-non-empty-queue bug (shouldn't happen, but) would be invisible. Recommend a low-frequency (e.g. every 30s) state/queue-depth log line, and ideally a periodic heartbeat to the server for dashboard visibility.

- [OPT] **`abortCurrent` does not notify the turtle or server (cross-cutting, Checks 3/7/8).** All four abort sites (no chests in RS line 315, arrival timeout 323, CHESTS_PLACED timeout 359, BATCH_DONE timeout 393) call `abortCurrent`, which sweeps the EC and resets to IDLE but emits **no JOB_ABORT** to the turtle/server. The turtle, if alive, only discovers the abort via its own timeout. Sending JOB_ABORT (as the server does in fwdToTurtle, and as the warehouse already does for unknown mid-job messages at line 279) would let the counterparty fail fast instead of waiting out its own deadline. Low risk because both sides have timeouts, but it lengthens recovery.

- [OK] **Mid-job messages for unknown jobs are correctly aborted back to the turtle (Check, recovery path).** Lines 263-282: CHESTS_PLACED/BATCH_DONE/ITEM_COLLECTED carrying a jobId not in `current` or `queue` trigger a JOB_ABORT reply to `msg.from` (line 279), so a turtle stuck re-sending after a warehouse reboot is told to clean up rather than looping forever. This is the warehouse-side mirror of the server's NOT-FOUND handling and is the correct recovery for the reboot-amnesia case. (It also means a CHESTS_PLACED arriving slightly *before* the entry is popped from queue could race — but the entry is checked against both `current` and `queue`, so an in-queue entry is recognized.)

---

## Layer 3: Dashboard / Bridge

### server.js

- [OK] **`pendingCommands` splice race is not a real race (Check 1).** Node is single-threaded with a run-to-completion event loop. The `/update` handler (line 145) is `async` but contains **no `await` before** `pendingCommands.splice(0)` (line 160) — the body runs synchronously from `const { turtles, jobs, version } = req.body` down to `res.json({...commands: pendingCommands.splice(0)})` without yielding. (The `upsertMarker(...).catch(()=>{})` at line 152 is fire-and-forget — it is **not** awaited, so it does not yield the handler.) Therefore two near-simultaneous `/update` POSTs are fully serialized: the first drains the whole array atomically and the second sees an empty array. No command is split across two responses, lost, or double-drained on the server side. **Caveat (real risk, but downstream):** commands are removed from `pendingCommands` the instant they are put in the response body, *before* the CC server has confirmed receipt. If the HTTP response is lost in transit (network drop, CC server crashes after sending the POST but before reading the body), those commands are gone forever — there is no ACK/redelivery. This is an [OVERSIGHT] at the protocol level (at-most-once delivery for commands like RECALL_ALL/CLEAR_JOBS), not a JS concurrency bug.

- [PERF/OVERSIGHT] **`state.turtles` grows unbounded; offline turtles are never pruned (Check 2).** `state.turtles[id]` is only ever written (line 151, `{ ...state.turtles[id], ...data }`) — there is no deletion path anywhere in `server.js`. This mirrors the `central_server.lua` registry finding: the bridge faithfully relays whatever the CC server pushes and never removes ids. Because the CC server itself never deletes registry entries (it only flips `online=false`), every turtle that has ever registered is pushed here and kept forever. Dashboard impact (confirmed in `public/index.html`): `renderFleet` (line 712) and `refreshTurtleSel` (line 777) iterate ALL of `turtles`, so a permanently-dead turtle stays in the fleet list as a greyed-out "OFFLINE" card (line 745) and in the turtle-select dropdown indefinitely. The map marker is only removed when the id vanishes from `turtles` (index.html line 626) — which never happens — so the stale Leaflet circle also persists (positioned at its last-known coords). The `online` flag the dashboard filters on (lines 695-696) is set entirely by the CC server; `server.js` does not compute liveness from `updatedAt`. Bounded in practice for a fixed fleet with stable `os.getComputerID` ids (re-registration overwrites the slot), but there is no decommission path and ghosts accumulate across the fleet's lifetime of churn.

- [OPT] **`markerExists` grows unbounded but is harmless (Check 3).** `markerExists[id]` (line 43) is set `true`/`false` per turtle id and never deleted. Same id-churn bound as `state.turtles` — at most one entry per distinct turtle id ever seen. It is a small boolean map keyed by id, so memory impact is negligible; functionally it just tracks add-vs-update for the Dynmap marker. No cleanup needed. (Minor coupling: it is never reset to remove a marker when a turtle is decommissioned, but since there is no decommission path, `removeMarker` (line 99) is never actually called from anywhere in this file — see PROTOCOL note below.)

- [PERF] **`rcon()` opens and tears down a fresh RCON connection on EVERY marker write (Check 4).** `rcon(cmd)` (lines 47-55) does `Rcon.connect()` → `send` → `client.end()` for a single command. In `/update`, `upsertMarker` is called once per turtle (line 152), and an *update* of an existing marker issues one `rcon` call (line 82) = one full TCP connect/auth/close. With N turtles pushed every ~2s, that is **N RCON connect+auth+close cycles every 2s** (and 2-3× N on the create/recreate path, lines 85-88, which fires up to three `rcon` calls for one marker). For a 10-turtle fleet that is ~5 connections/sec sustained, ~15+/sec on first push when markers are being created. Each connection re-runs the RCON auth handshake against the MC server. Risks: (a) RCON connection/auth overhead dominates; (b) some servers rate-limit or cap concurrent/total RCON logins and will start refusing connections under this churn (auth failures → markers silently stop updating via the swallowed catch, see Check 9); (c) connect/end is async and these calls are fired un-awaited and in parallel across all turtles in the for-loop, so a burst of simultaneous connects hits the server at once. A single persistent/pooled RCON client reused across calls (connect once at startup, reconnect on error) would eliminate the churn.

- [PERF/OVERSIGHT] **`proxyDynmap` has no timeout on the upstream `http.get` (Check 5).** `proxyDynmap` (lines 111-118) and the `/dynmap-frame` handler (lines 135-140) call `http.get('http://127.0.0.1:8123...')` with no `timeout` option and never call `req.setTimeout(...)`. Node's default socket timeout for `http.get` is effectively none (no timeout) — the `'error'` handler only fires on connection-level errors (ECONNREFUSED if Dynmap is down). If Dynmap is **up but slow/hung** (accepts the socket but never responds), the client browser request hangs indefinitely: the upstream socket stays open, `res` is never written, and the browser tab spins until the *browser's* own timeout. Worse, every such hung tile/asset request holds an open upstream socket and an open client response; under a Dynmap stall the dashboard can pile up many stuck connections. If Dynmap is fully **down**, `'error'` fires immediately → `res.status(404).end()` (proxy) / `502` (frame), which is fine. Recommend `http.get(url, { timeout: 5000 }, ...)` plus an `'timeout'` handler that calls `req.destroy()` and `res.status(504).end()`.

- [BUG] **`state.version` is undefined until the first CC push (Check 6).** The initial `state` object (lines 36-40) declares `turtles`, `jobs`, `updatedAt` but **not** `version`. It is only created when a push includes one: `if (version) state.version = version` (line 157). So before the first `/update` (or if CC never sends `version`), `GET /state` (line 165) returns an object with `version` absent (`undefined`, omitted from JSON). Dashboard handling (index.html line 675): `if (d.version) document.getElementById('cc-version').textContent = \`v${d.version}\`` — the guard means the version span keeps its placeholder `—` (index.html line 232) rather than showing a wrong value, so the header just shows a dash until the first push lands. Not a crash; cosmetic. Initialize `version: null` in the `state` literal for clarity/consistency with the other fields.

- [OVERSIGHT] **Default `express.json()` 100kb body limit could reject a large fleet push (Check 7).** `app.use(express.json())` (line 16) uses the default `limit: '100kb'`. The `/update` body is `{ turtles, jobs, version }` where `turtles` is the full registry (every turtle: x/y/z/status/fuel/role/jobId/dock/online ≈ a few hundred bytes serialized each) and `jobs` is the full job array. For a small fleet this is a few KB. But it scales linearly: a large fleet (dozens of turtles) plus a long jobs queue, each job carrying `params.destination` and metadata, could approach or exceed 100kb. If it does, Express responds **413 Payload Too Large** and the route never runs — the CC server's push silently fails (it likely just logs "Bridge push failed" per the central_server audit) and the dashboard goes stale with no on-screen indication. Set an explicit generous limit, e.g. `express.json({ limit: '5mb' })`, to remove the cliff. Currently latent for the present fleet size but a scaling landmine.

- [OVERSIGHT] **No authentication on `/command` (or any route) — intentional but unguarded (Check 8).** `POST /command` (lines 169-175) accepts any `{ type, params }` and queues it for the CC server to execute, with no auth token, origin check, or rate limit. Anyone who can reach port 3000 can queue `RECALL_ALL`, `CLEAR_JOBS`, `DISPATCH_DELIVERY`, `UPDATE_ALL` (OTA reboot), etc. `/update` is equally open — anyone can POST fake turtle state and poison the registry/markers, and can also **drain** `pendingCommands` (the `/update` response returns and clears them), so an attacker polling `/update` could steal/cancel legitimately-queued dashboard commands. Given this is a LAN-only hobby dashboard with a hardcoded RCON password in source (line 26), the lack of auth is presumably intentional. Flagging as [OVERSIGHT]: if the box is ever exposed beyond the trusted LAN, this is a full unauthenticated control plane for the turtle fleet and the Minecraft server (via queued commands). At minimum bind to localhost or a private interface and/or add a shared-secret header.

- [BUG/OVERSIGHT] **`upsertMarker` swallows errors and can wedge `markerExists[id]` into a permanent recreate loop (Check 9).** In `/update`, marker calls are fired as `upsertMarker(...).catch(()=>{})` (line 152) — the outer rejection is silenced. Inside `upsertMarker`, the `try` already catches RCON failures (line 93) and **sets `markerExists[id] = false`** (line 95) on any error, logging to console. Consequences: (a) if RCON is down, every marker write throws, `markerExists[id]` is forced back to `false`, and the next push takes the **create** branch (line 83) instead of update — so once RCON recovers, the marker will be re-created (or hit the delete+add recreate path, lines 87-88), which is actually self-healing. (b) The genuinely bad state: an error during the *create* branch sets `markerExists[id]=false` (line 95) even though a partial `dmarker add` may have *succeeded* on the server — the next push re-enters create, the plain `add` throws "marker already exists" (caught at line 86), and the fallback does `delete` then `add` (lines 87-88). That recreate path works but means a flapping RCON connection causes the marker to be deleted and re-added on every single push (visual flicker on the map, extra RCON load — compounding Check 4). (c) Errors are only visible in the server console (`console.error` line 94); the dashboard has no signal that markers stopped updating — they just freeze at last-known position. The double-swallow (inner catch sets state + logs, outer `.catch(()=>{})` discards the resolved-undefined) means a transient RCON blip silently degrades the map with no surfaced error.

- [PROTOCOL] **`removeMarker` is dead code — defined, never called (Check 3/9-adjacent).** `removeMarker(id)` (lines 99-107) is the only path that would delete a Dynmap marker and reset `markerExists[id]=false` cleanly, but nothing in `server.js` ever calls it. Combined with the no-prune findings (Checks 2/3), a turtle's marker, once created, is never intentionally removed — it lingers on the map at its last position for the life of the process. Either wire `removeMarker` into an offline/decommission path or remove it.

- [OVERSIGHT] **`/update` partial-payload handling is permissive and mostly safe, but a `version`-only push is rejected (Check 10).** The guard is `if (!turtles && !jobs) return 400` (line 147). So: (a) `turtles` without `jobs` → fine, jobs untouched (the `if (jobs)` at line 156 is skipped, `state.jobs` keeps its prior value). (b) `jobs` without `turtles` → fine, the turtle loop (line 149) is skipped. (c) `version` missing → fine, `state.version` simply isn't updated (line 157 guarded). (d) **Edge case:** a push of `{ version: "1.3.37" }` with neither `turtles` nor `jobs` is **rejected 400** even though it carries valid data — `version` alone can never update `state.version` because the guard fails first. Unlikely in practice (CC always sends turtles+jobs), but the guard should arguably also accept a version-only heartbeat, or at least the guard logic (`!turtles && !jobs`) should be documented as "must carry at least one of turtles/jobs." Also note `if (turtles)` / `if (jobs)` are truthiness checks: an explicitly-empty `turtles: {}` is **falsy-skipped? no** — `{}` is truthy, so an empty object enters the for-loop harmlessly (zero iterations); but an empty `jobs: []` is also truthy and correctly sets `state.jobs = []` (a real "all jobs cleared" push). Behavior is correct for empty collections. No crash on any missing-field combination; only the version-only case is a (benign) false-400.

### public/index.html

- [BUG] **Server-provided strings injected unsanitized into `innerHTML` — stored-XSS via turtle/job fields (Check 1).** `renderFleet` (line 720) and `renderJobs` (line 758) build their cards with template literals assigned to `el.innerHTML`. The interpolated values come straight from poll state with **no escaping**: turtle `id` (lines 735, 728/733 — also embedded inside an `onclick="setTurtleIdle('${id}')"` / `focusTurtle('${id}')` attribute), `t.role` (730, 736), `t.dock` (736), `t.status` (738), `t.jobId` (744), and on the jobs side `j.id` (767, and inside `onclick="cancelJob('${j.id}')"` line 761), `j.type` (768), `j.status` (763/770), `j.assignedTo` (772). `refreshTurtleSel` (line 782) is the same pattern for the `<option>` text/`value`. Any of these reaching the dashboard as an attacker-controlled string (a turtle whose `os.getComputerLabel()` or reported `dock`/`role`/`jobId` contains `</div><img src=x onerror=...>` or a `'` to break out of the `onclick` attribute) executes script in the dashboard origin. The server is on a private LAN and the values currently originate from trusted CC code, so exploitability is low, but every value is rendered raw — there is no `escapeHtml()` helper anywhere in the file. The `onclick` attribute injection (single-quote breakout in `id`/`j.id`) is the more realistic vector since IDs flow into inline JS. Recommend an HTML-escape helper for text and JSON-encoding/`data-` attributes + `addEventListener` for the click handlers. Note the rest of the UI is safe: `focusTurtle`/`toast` use `.textContent` (lines 790, 892) and the version span uses `.textContent` (line 675).

- [OVERSIGHT] **Dispatch modal sends coords via `sendCmd`→JSON, but they are `parseInt` (not `Number`) and the bridge forwards them as JSON numbers — OK on the wire, with one quirk (Check 2).** `submitDelivery` (lines 831-835) does `parseInt(...value)` for x/y/z, then `sendCmd('DISPATCH_DELIVERY', { x, y, z, note })` which `JSON.stringify`s them (line 884). Because `parseInt` yields a JS **number**, they serialize as bare JSON numbers (`"x":143`), and the CC server `tonumber(p.x)` (central_server.lua:839) handles them — so the "strings reach Lua" failure mode the check warns about does **not** occur here; numbers are sent, not strings. The task's concern about raw strings is satisfied (just via `parseInt` rather than `Number`). Minor quirks: (a) `parseInt('143.9')` truncates to `143` and `parseInt('1e3')` returns `1`, so non-integer or exponential input is silently mangled — `Number()` would be more faithful, though integer block coords make this moot. (b) `parseInt(y) || 67` (line 832) means a legitimately-entered `y=0` becomes `67` (0 is falsy); harmless for this depot (floor is 67) but technically wrong. (c) The Commands-tab quick-fields (`cx/cy/cz`, lines 822-825) are copied as raw `.value` **strings** into the modal inputs, but `submitDelivery` re-`parseInt`s them on submit, so they too end up numeric. No bug; numbers are transmitted.

- [OVERSIGHT] **Poll failure shows "server offline" but silently keeps stale data and a frozen `synced Ns ago` (Check 3, 10).** `poll` (lines 667-681) wraps the fetch in try/catch: on success it sets `status-dot` green and `last-update` = `synced ${ago}s ago` where `ago = (Date.now() - d.updatedAt)/1000`; on fetch failure it flips `status-dot` to `.offline` (red) and sets `last-update` = `server offline`. So a **bridge/network** outage IS visibly indicated (red dot + text) and does not crash — good. **The gap is the CC-server-stale case (Check 10):** if the Node bridge stays up but the CC server stops pushing (`/state` still responds 200 with an old `updatedAt`), the fetch **succeeds**, so the dot goes green and the header shows `synced 240s ago` — the number climbs but there is **no threshold that turns the indicator red/amber on staleness**. The operator must read the literal "240s ago" and notice it is large; nothing visually alarms. The `ago` value is computed but never compared against a freshness budget. Recommend: if `ago` exceeds e.g. 10s, set the dot to offline/amber and/or annotate "STALE". Also note `renderAll` is wrapped in its own try/catch (line 676) so a render exception can't break the poll loop — good resilience. (Aside: `Date.now()` is browser-local wall-clock vs the bridge's `updatedAt` (`server.js` sets it server-side); clock skew between the two machines directly biases the displayed `ago`.)

- [OVERSIGHT] **Modal can be left open with the iframe still hidden on a `submitDelivery` network failure (Check 4).** `showModal` (805-813) sets `dynmap-frame` to `visibility:hidden` and shows the overlay/backdrop/modal; `closeModal` (815-820) reverses all four. Exit paths audited: (a) backdrop click → `onclick="closeModal()"` (line 300) ✓; (b) CANCEL button → `closeModal()` (316) ✓; (c) DISPATCH → `submitDelivery` calls `closeModal()` at line 836 ✓. **Two gaps:** (1) **No Escape-key handler** — there is no `keydown` listener anywhere, so pressing Esc does nothing (the modal is a custom div, not a `<dialog>`), leaving the iframe hidden until the user clicks CANCEL/backdrop. Not a leak per se (user can still click out), but the brief's Esc path is unhandled. (2) **`submitDelivery` `await sendCmd(...)` then `closeModal()` (lines 835-836):** `sendCmd` itself swallows fetch errors in its own try/catch (line 886) and resolves normally, so `await` never throws and `closeModal()` always runs — **so this path is actually safe.** The real exposure: the `isNaN` early-return (line 834) happens *before* `closeModal`, which is correct (we want the modal to stay open to fix bad input), and the iframe is correctly still hidden then — but that is the intended "stay open" case, not a leak. Net: no path leaves the iframe permanently hidden, but the missing Esc handler is an ergonomic oversight worth noting.

- [OK] **Tab switching sets up no listeners or intervals — no leak (Check 5).** `switchTab` (656-661) only toggles CSS `active` classes on the tab headers and `.tab-content` panes; it does not attach event handlers, does not touch the poll interval, and does not re-render. The single `setInterval(poll, 2000)` (line 682) is created **once** at script load and never cleared or duplicated — switching tabs has zero effect on it. The card `onclick` handlers are inline attributes rebuilt fresh on each `innerHTML` assignment (so old DOM nodes and their inline handlers are GC'd, no accumulation). No duplicate listeners, no interval churn. Clean.

- [OK] **Version display sourced from `state.version`, guarded, placeholder `—` until first push (Check 6).** Poll handler: `if (d.version) document.getElementById('cc-version').textContent = \`v${d.version}\`` (line 675). The value comes from `/state`'s `version` field, which the bridge only sets after the CC server pushes one (`server.js` line 157, `if (version) state.version = version`). Before the first push, `d.version` is `undefined`, the `if` guard skips, and the header keeps its literal placeholder `—` (line 232, the `#cc-version` span's initial text). So the header shows a dash (not `vundefined`) until the first successful push, then `v1.3.37`. Correct, no flicker to a wrong value. Pairs with the `server.js` Check 6 finding (`state.version` undefined until first push) — the dashboard guard is what makes that benign.

- [OK] **Per-item CANCEL sends `CANCEL_JOB` with the correct `jobId` (Check 7).** The job card renders `onclick="cancelJob('${j.id}')"` (line 761), and `cancelJob(jobId)` (856-860) confirms then `sendCmd('CANCEL_JOB', { jobId })`. `sendCmd` posts `{ type:'CANCEL_JOB', params:{ jobId } }` (line 884). On the CC side `handleBridgeCommand` reads `p.jobId` for `CANCEL_JOB` (central_server.lua:868-869) and calls `server.cancelJob(jobId)`. Type string and param name match exactly. The `jobId` is the rendered card's own `j.id` (the server-assigned job id), passed directly — extracted correctly, not parsed out of DOM text. Only caveat is the XSS/quote-breakout noted in Check 1 if `j.id` ever contained a `'`.

- [OK] **Per-item IDLE sends `SET_IDLE` with the correct `turtleId` (Check 8).** Turtle card renders the IDLE button only when `st !== 'IDLE'` (lines 727-729) as `onclick="setTurtleIdle('${id}')"`, and `setTurtleIdle(id)` (861-864) calls `sendCmd('SET_IDLE', { turtleId: id })` → posts `{ type:'SET_IDLE', params:{ turtleId } }`. CC `handleBridgeCommand` reads `p.turtleId` for `SET_IDLE` (central_server.lua:879-883), validates `state.registry[tid]`, sets status IDLE + clears jobId. Type and param name match. `id` is the registry key from `Object.entries(turtles)` (line 720), passed directly — correct extraction. (Note: unlike CANCEL_JOB there is **no `confirm()`** on the per-item IDLE — a single click forces that turtle IDLE with no undo; minor [OPT], it just clears the server's view, the turtle keeps running its job coroutine.)

- [OK] **Global command type strings all match `handleBridgeCommand` (Check 9).** Verified each dashboard `sendCmd` type against the CC dispatcher: CLEAR ALL JOBS → `cmdClearJobs` → `'CLEAR_JOBS'` (line 872) == central_server.lua:898; SET ALL STATUS TO IDLE → `cmdResetStatus` → `'RESET_STATUS'` (867) == central_server.lua:889; RECALL SELECTED → `'RECALL'` {turtleId,reason} (848) == :852; RECALL ALL → `'RECALL_ALL'` {reason} (853) == :861; OTA UPDATE ALL → `'UPDATE_ALL'` (877) == :864; DISPATCH → `'DISPATCH_DELIVERY'` (835) == :838. All six type strings and their param shapes (`turtleId`, `reason`, `jobId`, `x/y/z/note`) line up with what the server destructures. No orphaned command. Each destructive global button is gated behind `confirm()` (lines 852, 866, 871, 876) — only the per-item IDLE and CANCEL-modal-less paths differ (CANCEL has its own confirm, line 857).

- [OPT] **`lastState` initialised as `{ turtles:{}, jobs:[] }` but `poll` overwrites it with the raw `/state` response (`lastState = d`, line 671), which may carry `jobs` as `{}` not `[].`** `renderAll` and `updateInjectedMarkers` both defensively normalise (`Array.isArray(d.jobs) ? d.jobs : Object.values(...)`, lines 692, 550-551), so the CC `{}`-for-empty-array quirk is handled everywhere it matters. No bug; noted because `lastState.jobs` is consumed raw in `updateInjectedMarkers` (line 550) and would break a naive `.find` if the guard were ever removed.

- [OPT] **Stale marker cleanup in `updateInjectedMarkers` (lines 624-631) depends on a turtle id *disappearing* from `turtles`, which never happens (cross-cut with `server.js`/`central_server.lua` no-prune findings).** Because the CC registry never deletes ids and the bridge never prunes, the `if (!turtles[id])` branch is effectively dead — markers for permanently-offline turtles linger on the map at last-known position. Same root cause already filed against `server.js` (removeMarker dead) and `central_server.lua` (registry unbounded); flagged here as the dashboard-side manifestation. The fleet list and turtle-select dropdown likewise show offline ghosts (greyed `.offline` card, line 730/107).

---

## Cross-Layer Protocol Check

Every `proto.MSG` key was traced to its transmitter(s) and handler(s) across all layers.

**Production handshakes — sender and handler both present, no gap:**
- `REGISTER`/`REGISTER_ACK`, `HEARTBEAT`/`HEARTBEAT_ACK`, `RECALL`, `JOB_ASSIGN`/`JOB_ACK`, `STATUS_UPDATE`, `JOB_COMPLETE`, `JOB_FAILED` — turtle↔server, all wired.
- `UPDATE_ALL` — sent by `handleBridgeCommand` (`central_server.lua:865`, `sendBroadcast`); handled by every turtle (`turtle_base.lua:1144`) and by the warehouse (`warehouse.lua:176`). Complete.
- `TURTLE_QUERY`/`TURTLE_INFO` — sent by `base.queryTurtle` (`turtle_base.lua:1031`, called from `support_turtle.lua:105`); handled at `central_server.lua:632`, reply consumed at `turtle_base.lua:1033`. Complete and in active use (support stall-guard).
- `JOB_REQUEST` — defined "admin UI → server." Transmitted by `admin_ui.lua:902` and `stress_test.lua:53`; handled at `central_server.lua:649`. **Used** (via tooling, not the dashboard — the dashboard dispatches via the bridge `DISPATCH_DELIVERY` command instead).
- Warehouse delivery handshake `WAREHOUSE_QUEUED`/`DELIVERY_ARRIVED`/`CHESTS_READY`/`CHESTS_PLACED`/`ITEMS_READY`/`BATCH_DONE`/`ITEMS_DONE`/`ITEM_COLLECTED` — all forwarded by server (`fwdToWarehouse`/`fwdToTurtle`) and consumed on both ends. Complete.
- CH_LOCAL pair follow signals `HOLE_READY`, `SUPPORT_STAGED`, `POSITION_UPDATE`, `ASCENDING`, `DESCENDED`, `RETURN_TO_DOCK`, `JOB_ABORT` — all sent (`turtle_base.lua`/`delivery_turtle.lua`) and handled (`support_turtle.lua`). Complete.

**Gaps confirmed (see Summary Table):**
- `SUPPORT_READY` — **defined, never sent, never handled.** Dead protocol; the real underground handshake is `HOLE_READY`→`SUPPORT_STAGED`. (Was already flagged in the support_turtle audit.)
- `ITEM_REQUEST`/`ITEM_READY` legacy pickup pair — **superseded and half-dead.** `ITEM_REQUEST` *is* live, but only as a warehouse **queue-slot** request: `delivery_turtle.lua:235,352` sends it, the server forwards it to the warehouse (`central_server.lua:585`), and the warehouse handles it (`warehouse.lua:199`). `ITEM_READY`, however, has **no production sender**: the warehouse never emits it (it uses `CHESTS_READY`/`ITEMS_READY` instead); the only `ITEM_READY` senders are the mock/test paths `central_server.lua:1018` (auto-mock) and `startup_server.lua:128` (manual unblock). The only turtle-side consumer of `ITEM_READY` is `base.requestItems` (`turtle_base.lua:1001-1012`), which is **never called by any job handler** — confirmed dead code. So the `ITEM_REQUEST`→`ITEM_READY` "items loaded, proceed" handshake the protocol comment describes does not exist in the live delivery flow; only the `ITEM_REQUEST`-as-queue-ping half survives. (New [PROTOCOL] finding #23 below; resolves the "pending confirmation" note in the turtle_base and protocol audits.)

**Bridge command cross-reference (dashboard ↔ `handleBridgeCommand`):** All 8 dashboard `sendCmd` types — `DISPATCH_DELIVERY`, `RECALL`, `RECALL_ALL`, `UPDATE_ALL`, `CANCEL_JOB`, `SET_IDLE`, `RESET_STATUS`, `CLEAR_JOBS` — have a matching case in `handleBridgeCommand` (`central_server.lua:838-912`), and every case has a corresponding dashboard button. No orphaned command in either direction.

---

## Summary Table

| # | Severity | File | Description |
|---|----------|------|-------------|
| 1 | CRITICAL | central_server.lua | Heartbeat-timeout `reassign`/`markOffline` never reset a dead turtle's status, and the reassigned delivery's linked support job is never cancelled — orphans the support partner (still SUPPORT_FOLLOW/non-IDLE) and can stall re-dispatch. |
| 2 | BUG | turtle_base.lua | Route-failure exits in `returnToDockInternal`/`returnToDock`/`depart` return false with status left mid-transition (e.g. RETURNING) and no recovery — a turtle stuck in an aisle reports RETURNING indefinitely. |
| 3 | BUG | turtle_base.lua | No return-trip fuel reserve: `FUEL_CRITICAL` is a flat 200 regardless of distance, so far-away jobs can pass the point of no return; mid-job exhaustion becomes a soft-hang instead of a clean failure. |
| 4 | BUG | turtle_base.lua | `ensureFuel` enters an unbounded `while fuel.isCritical()` loop on an empty/unreachable fuel chest — turtle parks forever sending ERROR heartbeats, no escalate-and-park path, locking the delivery pair. |
| 5 | BUG | delivery_turtle.lua | All deadlines use `os.clock()` (CPU time, runs slow under parallel coroutines) instead of `os.epoch("utc")` — the 600s queue deadline and 60s re-send interval expire late in wall-clock terms and desync from the warehouse's wall-clock timeouts. |
| 6 | BUG | delivery_turtle.lua | First-phase (WAIT_ARRIVE) wait loop has no `isRecalled()` check — a RECALL does not abort it; the turtle keeps pinging DELIVERY_ARRIVED for the full 600s deadline while already reporting IDLE/failed to the server (recent v1.3.35 regression). |
| 7 | BUG | delivery_turtle.lua | `chestIdx` is never reset between batches and only increments; once it passes a chest it never returns, and when `chestIdx > #chestPositions` a pulled batch is never dropped into any chest — items ride home undelivered, with BATCH_DONE still sent and no error. |
| 8 | BUG | delivery_turtle.lua | Phase-2 batch loop has no overall wall-clock deadline — if the warehouse goes silent (no ITEMS_DONE, no abort) the turtle re-pings CHESTS_PLACED forever with no escape. |
| 9 | BUG | support_turtle.lua | POSITION_UPDATE `prev.x/.y/.z` are not validated before `move.to` — a malformed/truncated packet (`prev={}` or partial) drives `move.to(nil,...)`, whose `while pos.x ~= nil` loop spins and walks the turtle off-route. |
| 10 | BUG | support_turtle.lua | No fuel check before the follow sequence; underground exhaustion hits the same unbounded `ensureFuel` block and parks support mid-follow, which also strands the delivery turtle it was chunk-loading. |
| 11 | BUG | central_server.lua | `pushToBridge` trusts the bridge blindly — non-idempotent commands (DISPATCH_DELIVERY, CANCEL_JOB) returned twice are executed twice; no de-dup/idempotency key on the CC side. |
| 12 | BUG | central_server.lua | `CLEAR_JOBS` recalls assigned turtles but does not cancel the matching `linkedJob`, orphaning in-flight warehouse handshakes (recovered only by the warehouse-abort fallback). |
| 13 | BUG | central_server.lua | ACK-timeout `reassign` sets a DELIVER job back to PENDING while its turtle may still be travelling; `destinationBusy` then returns false and a second pair can be dispatched to the same destination (two delivery turtles converge). |
| 14 | BUG | central_server.lua | `state.registry` grows unbounded — `markOffline` only sets `online=false`; there is no `registry.remove`/decommission path, so offline ghosts persist forever and are pushed to the dashboard every cycle. |
| 15 | BUG | warehouse.lua | DELIVERY_ARRIVED auto-queue de-dups on `turtleId` while ITEM_REQUEST de-dups on `jobId` — a stale same-turtle entry blocks a cancelled+reissued job's new jobId, which then gets served under the old jobId and aborted by the server's NOT-FOUND path. |
| 16 | BUG | warehouse.lua | `clearEnderChest` is single-shot: lists once, imports once, no re-list/verify/retry — if `rsBridge.importItem` can't move an item, residue silently remains and is later pulled by the next turtle as "debris." |
| 17 | BUG | warehouse.lua | Timeout-aborts (`abortCurrent`) emit no JOB_ABORT to the turtle/server, so a legitimately-slow turtle is dropped from the queue silently and only discovers it via its own deadline (lengthened recovery). |
| 18 | BUG | warehouse.lua | Item export under-delivers silently: `checkStock` only WARN-logs a shortfall and the caller ignores its return; a short/missing item is exported as-is (or zero) with no abort, re-queue, or shortfall signal to the server. |
| 19 | BUG | warehouse.lua | `WAREHOUSE_QUEUED` position is a one-shot snapshot sent at enqueue and never re-emitted as the queue drains — waiting turtles display a stale, monotonically-wrong position. |
| 20 | BUG | server.js | `state.version` is undefined until the first CC push (not initialized in the `state` literal) — `GET /state` omits `version` until then. (Benign: the dashboard guards on it.) |
| 21 | BUG | server.js | `upsertMarker` errors are double-swallowed (inner catch sets `markerExists[id]=false`, outer `.catch(()=>{})`); a flapping RCON connection causes delete+re-add of the marker every push (flicker, extra RCON load) and the dashboard gets no signal that markers froze. |
| 22 | BUG | public/index.html | Server-provided strings (turtle `id`/`role`/`dock`/`status`/`jobId`, job `id`/`type`/`status`/`assignedTo`) are interpolated raw into `innerHTML` and inline `onclick` attrs with no escaping — stored-XSS / single-quote attribute-breakout vector (low exploitability on trusted LAN, but no `escapeHtml` exists). |
| 23 | PROTOCOL | turtle_base.lua / warehouse.lua | Legacy `ITEM_REQUEST`→`ITEM_READY` pickup handshake is dead: `ITEM_READY` has no production sender (warehouse uses CHESTS_READY/ITEMS_READY; only mock paths emit it) and its sole consumer `base.requestItems` is never called. Only `ITEM_REQUEST`-as-queue-ping survives. |
| 24 | PROTOCOL | support_turtle.lua / protocol.lua | `SUPPORT_READY` is defined in `proto.MSG` but never sent and never handled anywhere — dead protocol (the real underground handshake is HOLE_READY→SUPPORT_STAGED). Wire it in or remove it. |
| 25 | PROTOCOL | server.js | `removeMarker` is defined but never called — the only clean marker-delete path is dead, so markers linger on the map forever (compounds the no-prune findings). |
| 26 | PROTOCOL | support_turtle.lua | `HOLE_READY`/`SUPPORT_STAGED` direction is the inverse of the task brief's one-line glosses; the implementation is internally consistent (no bug) but the protocol comments mislead about who stages first. |
| 27 | OVERSIGHT | protocol.lua | `proto.send` calls `textutils.serialise` on every send; throws (crashes the sender) on payloads with functions/recursive tables rather than failing gracefully, and the raw-table branch in `proto.receive` is dead. |
| 28 | OVERSIGHT | protocol.lua | `proto.receive` silently drops malformed/garbage modem messages (`unserialise`→nil guard) with no log — failures are invisible for debugging. |
| 29 | OVERSIGHT | waypoints.lua | Hole coordinate literals (143/-2813, 228/-2782) are duplicated across `DISPATCH_HOLE`/`DISPATCH_STAGING`/`WORLD_EXIT` and `ARRIVALS_HOLE`/`WORLD_ENTRY` instead of deriving WORLD_EXIT/WORLD_ENTRY from the hole tables — a layout move risks vertical misalignment. |
| 30 | OVERSIGHT | waypoints.lua | Route builders (`internalReturnRouteFrom`, `returnAisleZ`, `dockFacing`, `departureRoute`, etc.) deref `dock.x/.z/.junction.x` with no internal nil guard — latent; only safe because every current caller guards at the call site. |
| 31 | OVERSIGHT | turtle_base.lua | Internal `gpsSync()` callers (`applyMove`, `depart`, `returnToDock*`, `init`) discard the return value, so a failed `gps.locate()` leaves the turtle dead-reckoning with no WARN logged; drift accumulates invisibly. |
| 32 | OVERSIGHT | turtle_base.lua | `bypassForward` floor gate uses the raw literal `66` instead of `CFG.FLOOR_Y` (67) — off-by-one in intent; works for the two real cases but is a magic number. |
| 33 | OVERSIGHT | turtle_base.lua | Floor-Y literals `66`/`67` are scattered across 5 sites and disagree (init "inside building" uses 66, RECALL uses 67 for the same predicate), so a turtle at y=66 is "inside" for init but "outside" for RECALL. |
| 34 | OVERSIGHT | turtle_base.lua | The "inside building" bounding box is hardcoded twice (init vs RECALL) and duplicates `W.DISPATCH_HOLE`/`W.ARRIVALS_HOLE` extents — should be a single `W.isInsideBuilding(pos)` helper. |
| 35 | OVERSIGHT | turtle_base.lua | `_self.facing` persists for the process lifetime and is never re-derived by GPS (only boot-time `detectFacing`); a physical bump or chunk reload desyncs heading until it produces a wrong move per axis. |
| 36 | OVERSIGHT | delivery_turtle.lua | `params.destination` and its `.x/.y/.z` are dereferenced with no guard — a nil destination throws a caught-but-opaque Lua error in `sendProgress`/`move.to`; recommend an explicit `missing_destination` `sendFailed`. |
| 37 | OVERSIGHT | delivery_turtle.lua | Chest placement has no slot-occupied / wrong-item / inventory-full safety: `placeDown()` returns are discarded, a non-chest item pulled from the EC is skipped and rides home as future debris, and under-pulls are invisible. |
| 38 | OVERSIGHT | delivery_turtle.lua | The entangled-chest `placeDownClear()` return is also ignored — if the EC fails to place (bedrock/protected/occupied), the debris-dump and every suck/drop run against air and the handshake stalls until the 600s timeout. |
| 39 | OVERSIGHT | delivery_turtle.lua | EC pickup on the abort paths is best-effort `digDown` with no `checkEC` verification (unlike the clean path's CHECKPOINT 3 + retry) — a mis-centred turtle leaves the EC in the world and returns without it, silently. |
| 40 | OVERSIGHT | support_turtle.lua | Movement (`move.to`) blocks the message loop, so ASCENDING/RETURN_TO_DOCK/JOB_ABORT are buffered and only acted on after the in-flight move completes — "hold immediately when delivery ascends" is not guaranteed. |
| 41 | OVERSIGHT | support_turtle.lua | `SUPPORT_STAGED` is fire-and-forget with no ACK; if the packet is lost, delivery burns its full 60s deadline then descends anyway while support already descended after a 1s sleep — pair desyncs and can collide at the hole, neither aborts. |
| 42 | OVERSIGHT | support_turtle.lua | Neither the HOLE_READY wait nor the follow loop checks `base.isRecalled()` — a RECALL leaves the job loop waiting up to 180s (or indefinitely in follow) while the server already thinks the job failed. |
| 43 | OVERSIGHT | support_turtle.lua | The follow-phase stall guard (`queryTurtle`) is skipped while `isServerDown()` and has no absolute wall-clock ceiling, so a crashed delivery partner during a server outage leaves support following nothing indefinitely. |
| 44 | OVERSIGHT | central_server.lua | `sendTo` for a nil/unknown/stale turtle id transmits into the void with no registry lookup and no warning — a lost message (e.g. to a removed `assignedTo`) is invisible. |
| 45 | OVERSIGHT | central_server.lua | `saveJobs` is not atomic and not pcall-wrapped — a non-serialisable job param throws (crashing the un-pcall'd console path), and an in-place truncating write can leave a partial `jobs.dat` (caught on next boot by the load-time type check). |
| 46 | OVERSIGHT | central_server.lua | `registry.checkTimeouts` forward-references `jobQueue` as an implicit global (no `local`) declared later in the file — works only by call-ordering luck and pollutes `_G`. |
| 47 | OVERSIGHT | central_server.lua | `jobQueue.complete` deliberately does not free the linked support turtle, so if that support turtle crashes between master-complete and docking its SUPPORT_FOLLOW job stays IN_PROGRESS forever (no destination → never GC'd), excluding it from `getIdle`. |
| 48 | OVERSIGHT | warehouse.lua | No periodic "stuck in state X for Ys" heartbeat/log and no STATUS sent to the server — once a wait-state is entered the warehouse is silent until the message or the (up-to-1800s) timeout, and the dashboard has zero warehouse visibility. |
| 49 | OVERSIGHT | server.js | Default `express.json()` 100kb body limit could 413-reject a large-fleet `/update` push, silently failing the CC push and going stale with no on-screen indication — set an explicit generous limit. |
| 50 | OVERSIGHT | server.js | No authentication on `/command` or `/update` (or any route) — anyone reaching port 3000 can queue RECALL_ALL/CLEAR_JOBS/UPDATE_ALL, poison state, or drain `pendingCommands`. Presumably intentional for LAN, but a full unauthenticated control plane if ever exposed. |
| 51 | OVERSIGHT | server.js | A `version`-only `/update` push is rejected 400 by the `if (!turtles && !jobs)` guard even though it carries valid data (benign; CC always sends turtles+jobs). |
| 52 | OVERSIGHT | public/index.html | Dispatch-modal coords use `parseInt` (truncates `143.9`→143, `1e3`→1, and `y=0`→67 via `|| 67`) — harmless for integer block coords but `Number()` would be faithful. |
| 53 | OVERSIGHT | public/index.html | Poll handles bridge-down (red dot) but has no CC-staleness threshold: if the bridge stays up while the CC server stops pushing, the dot stays green and "synced Ns ago" just climbs with no amber/red alarm. |
| 54 | OVERSIGHT | public/index.html | No Escape-key handler for the dispatch modal (custom div, not `<dialog>`) — only CANCEL/backdrop/DISPATCH close it; the iframe stays hidden until one of those. |
| 55 | PERF | central_server.lua | `http.post` to the bridge is blocking/yielding with no `parallel` wrapper — every 2s the whole `server.run` loop stalls for one full HTTP round-trip; any bridge stall delays heartbeat ACKs and can push turtles toward HEARTBEAT_TIMEOUT. |
| 56 | PERF | warehouse.lua | Unconsumed DELIVERY_ARRIVED messages for the currently-served turtle accumulate in `inbox[DELIVERY_ARRIVED]` forever (bucket never swept on completion) — slow unbounded growth, and a stale entry could mis-satisfy a later job's WAIT_ARRIVE. |
| 57 | PERF | server.js | `state.turtles` grows unbounded — written but never deleted; mirrors the CC registry no-prune, so the fleet list, dropdown, and map markers accumulate offline ghosts forever. |
| 58 | PERF | server.js | `rcon()` opens+auths+closes a fresh RCON connection on every marker write — for an N-turtle fleet that is N (to 3×N) connect/auth/close cycles every 2s; risks RCON rate-limits and connection churn. A pooled/persistent client would eliminate it. |
| 59 | PERF | server.js | `proxyDynmap`/`/dynmap-frame` call `http.get` with no timeout — if Dynmap is up-but-hung the browser request and the upstream socket hang indefinitely, piling up stuck connections. Add `{ timeout: 5000 }` + a `timeout` handler. |
| 60 | OPT | protocol.lua | `proto.receive` timer branch reuses modem field names (`side` actually holds the timer ID) — correct but misleading; clearer naming or a dedicated timer pull would prevent future bugs. |
| 61 | OPT | delivery_turtle.lua | `lastIReq` is never reset once the first phase ends, but its scope is confined to the queue loop so no stale-timer misfire occurs — noted for completeness (reset it if the loop is ever refactored to re-enter). |
| 62 | OPT | support_turtle.lua | Debug `print` of params on every job and several prints in the hot follow loop are noisy; the `destination` param is read but never used for navigation (support follows POSITION_UPDATEs) — dead plumbing. |
| 63 | OPT | support_turtle.lua | `POLL_INTERVAL` and `UNDERGROUND_Y` locals are declared but unused — remove or wire in for consistency. |
| 64 | OPT | central_server.lua | The main loop dispatches `key`/`char` console events with no pcall — a throw inside `handleConsoleEnter` (e.g. a `saveJobs` serialise error) crashes `server.run`; wrap it like the timer/handler paths. |
| 65 | OPT | warehouse.lua | `abortCurrent` does not notify the turtle/server on any of its four abort sites — sending JOB_ABORT would let the counterparty fail fast instead of waiting out its own deadline. |
| 66 | OPT | server.js | `markerExists` grows unbounded but is harmless (a small boolean map keyed by stable turtle id, bounded by fleet size). |
| 67 | OPT | public/index.html | `lastState` is overwritten with the raw `/state` response (which may carry `jobs` as `{}` not `[]`); consumers defensively normalise, so no bug — noted because a removed guard would break a naive `.find`. |
| 68 | OPT | public/index.html | Stale-marker cleanup in `updateInjectedMarkers` keys on a turtle id *disappearing* from `turtles`, which never happens (no-prune cross-cut) — the cleanup branch is effectively dead; offline ghosts linger on the map and in the fleet list/dropdown. |
| 69 | OPT | public/index.html | Per-item IDLE button has no `confirm()` (unlike per-item CANCEL) — a single click forces a turtle IDLE on the server with no undo (the turtle keeps running its job coroutine). |
